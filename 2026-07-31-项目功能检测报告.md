# 项目功能检测报告

> **检测时间**:2026-07-31
> **检测范围**:xianyu-assistant-package-temp 全量代码(core-api / automation-service / crawler-service / user-web / admin-web / 数据库迁移 / 部署配置)
> **检测方法**:并行子代理分模块审查 + 规则一致性校验
> **检测依据**:.trae/rules 工作区规则、project_memory.md 硬约束、各模块设计文档

---

## 一、问题汇总

本次检测共发现 **48 个潜在问题**,按严重程度分布如下:

| 严重程度 | 数量 | 分布 |
|---------|------|------|
| 高(P0) | 10 | core-api 3 / automation-service 2 / crawler-service 2 / 数据库迁移 5 |
| 中(P1) | 21 | user-web 2 / admin-web 13 / crawler-service 4 / 数据库迁移 4(去重后) |
| 低(P2) | 17 | 各模块零散问题 |

**最关键的 5 个问题**(必须立即修复):

1. `_auto_confirm_shipment` 同步调用阻塞事件循环 → 整个 automation-service 卡死
2. `server.ts` 第 840 行 `timeoutMs` 默认值 90000 违反硬约束 → 滑块求解重试次数耗尽
3. core-api 37 处 `ObjectMapper` 未注册 `JavaTimeModule` → 含时间字段的接口 500
4. `DeliveryExecutionService` 去重逻辑不完整 → 发货死循环事故复发
5. `.env.production.example` 环境变量名错误 → 生产环境运行时 schema 变更开关失效

---

## 二、高严重度问题(P0,必须立即修复)

### 2.1 `_auto_confirm_shipment` 同步调用阻塞事件循环

- **文件**:[ws_delivery_handler.py](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/app/services/ws_delivery_handler.py#L2594-L2648)
- **行号**:第 2611 行
- **问题描述**:
  `_auto_confirm_shipment` 是 `async def` 异步函数,但内部直接同步调用 `confirm_order_shipment`(发起 MTOP HTTP 请求的同步函数),**未使用 `asyncio.to_thread` 包装**,导致事件循环在 HTTP 请求期间被完全阻塞。

  同代码库中已有 4 处正确用法(`ws_delivery_handler.py` 第 362、2515、1347 行、`internal.py` 第 490 行均使用 `await asyncio.to_thread(...)`),唯独此处遗漏。

- **影响范围**:
  - 被 `_execute_text_delivery`(第 954 行)和 `_execute_kami_delivery`(第 1982 行)调用,位于发货流程必经路径
  - MTOP HTTP 请求耗时较长时(网络抖动/重试),整个 automation-service 事件循环冻结:
    - 所有账号 WS 心跳停滞(15s 超时断连)
    - 其他账号付款消息排队等待
    - 滑块求解队列卡死
    - FastAPI 路由处理器无法响应
    - 异步数据库操作排队(加剧发货死循环)
- **严重程度**:高
- **建议修复**:
  ```python
  result = await asyncio.to_thread(
      confirm_order_shipment,
      account_id, order_id,
      is_bargain=is_bargain,
      item_id=xy_goods_id,
      buyer_id=buyer_user_id,
  )
  ```
  同时修复 [order.py](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/app/api/v1/routes/order.py#L112) 第 112、146 行的同类问题。

---

### 2.2 crawler-service `server.ts` 滑块求解超时参数违反硬约束

- **文件**:[server.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/server.ts#L839-L840)
- **行号**:第 839、840 行
- **问题描述**:
  ```typescript
  maxRetries: Math.max(1, Math.min(Number(maxRetries) || 5, 8)),   // 上限 8,应为 5
  timeoutMs: Math.max(5000, Math.min(Number(timeoutMs) || 90000, 180000)),  // 默认 90000,应为 30000
  ```
  违反 `project_memory.md` 硬约束:"crawler-service maxRetries 必须保持 5,timeoutMs 必须保持 30000"。

  `sliderSolver.ts` 第 1724-1727 行已修复(默认 5 / 30000),但 `server.ts` 入口默认值仍是旧值,**修复不完整**。

- **影响**:Python 调用方不显式传参时,触发 5×90s=450s 远超 httpx 180s 超时,导致"httpx 先超时、retry_count=0、5 次重试根本没机会执行"事故(2026-07-29 已发生过)。
- **严重程度**:高
- **建议修复**:
  ```typescript
  maxRetries: Math.max(1, Math.min(Number(maxRetries) || 5, 5)),
  timeoutMs: Math.max(5000, Math.min(Number(timeoutMs) || 30000, 180000)),
  ```

---

### 2.3 core-api 37 处 ObjectMapper 未注册 JavaTimeModule

- **文件**:core-api 目录下 41 处 `new ObjectMapper()`,其中 37 处未注册 `JavaTimeModule`
- **关键问题位置**:

  | 文件 | 行号 | 影响 |
  |------|------|------|
  | [SyncReceiveService.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/SyncReceiveService.java#L33) | 33 | 同步数据含 created_time 序列化失败 |
  | [AiCsService.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/AiCsService.java#L43) | 43 | AI 客服会话时间字段序列化失败 |
  | [AiBillingService.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/AiBillingService.java#L36) | 36 | 计费日志时间字段序列化失败 |
  | [AutomationProxyController.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/controller/AutomationProxyController.java#L63) | 63 | SSE 票据等序列化失败 |
  | [BusinessSettingsService.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/BusinessSettingsService.java#L40) | 40 | 业务配置时间字段序列化失败 |
  | [JwtUtil.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/util/JwtUtil.java#L23) | 23 | JWT claims 时间字段失败 |
  | [ModelConfigService.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/ModelConfigService.java#L33) | 33 | 模型配置时间字段失败 |
  | 其他 30 处 | - | 各类业务场景时间字段失败 |

- **问题描述**:全局无 `JacksonConfig` 类,只有 4 处正确配置(`AutomationClient.java`、`WorkflowService.java`、`DataSyncService.java`、`DeliveryGlobalConfigController.java` 使用 `findAndRegisterModules()` 或 `registerModule(new JavaTimeModule())`)。剩余 37 处未注册,任何包含 `LocalDateTime` 的对象通过这些 ObjectMapper 序列化时会抛 `InvalidDefinitionException`,导致接口 500 错误。
- **严重程度**:高
- **建议修复**:创建全局 Jackson 配置 Bean:
  ```java
  @Configuration
  public class JacksonConfig {
      @Bean
      public ObjectMapper objectMapper() {
          return new ObjectMapper()
              .registerModule(new JavaTimeModule())
              .disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
      }
  }
  ```
  将所有 `new ObjectMapper()` 替换为注入该 Bean,或至少统一改为 `new ObjectMapper().findAndRegisterModules()`。

---

### 2.4 DeliveryExecutionService 去重逻辑不完整(发货死循环风险)

- **文件**:[DeliveryExecutionService.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/DeliveryExecutionService.java#L153-L168)
- **行号**:第 153-168 行
- **问题描述**:
  重复发货最后防线 SQL 仅查询 `status=2`(成功)记录:
  ```sql
  SELECT COUNT(*) FROM delivery_record
  WHERE tenant_id=? AND order_id=? AND deleted=0 AND status=2 AND id<>? LIMIT 1
  ```
  未考虑 `status=3`(失败)记录的时间窗口去重。

- **违反约束**:`project_memory.md` 明确要求"必须同时考虑 status IN (1,2) 成功记录与 status=3 失败记录(在合理时间窗口内),不得仅匹配成功记录。失败记录参与去重的最小窗口为 1 小时"。
- **影响**:闲鱼 WS 周期性推送付款消息触发 `_process_delivery`,写入 `status=3` 失败记录后,下一次推送时这条失败记录不会被去重命中,形成"WS 推送 → 发货失败 → 不去重 → 闲鱼再推送 → 再失败"的死循环。2026-07-28 已发生过 220+ 条失败记录事故。
- **严重程度**:高
- **建议修复**:
  ```sql
  WHERE tenant_id=? AND order_id=? AND deleted=0
  AND ((status IN (1,2) AND id<>?)
       OR (status=3 AND created_time >= DATE_SUB(NOW(), INTERVAL 1 HOUR)))
  LIMIT 1
  ```
  同时在末尾增加 `xianyu_trade_order.order_status=3` 强校验作为第二道防线。

---

### 2.5 StartupSecurityGuard 默认 opt-in(fail-open 设计)

- **文件**:[StartupSecurityGuard.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/config/StartupSecurityGuard.java#L39-L41)
- **行号**:第 39-41 行
- **问题描述**:
  ```java
  if (!Boolean.parseBoolean(property("xianyu.security-guard.enabled", "false"))) {
      return;
  }
  ```
  即使在 prod/staging 环境,不显式设置 `XIANYU_SECURITY_GUARD_ENABLED=true` 时,所有安全检查(token 强度、ADMIN_SEED_ENABLED、SCHEMA_RUNTIME_MUTATIONS_ENABLED)都不执行。

- **影响**:
  - `data-sync-bridge-token.md` 规则要求的 token 强度校验形同虚设
  - `local-dev-default-credentials.md` 规则要求的"生产环境 ADMIN_SEED_ENABLED=false"校验失效
  - 生产环境忘记配置时,弱 token、生产环境种子初始化等事故级 Bug 不会被拦截
- **严重程度**:高
- **建议修复**:生产环境默认启用:
  ```java
  boolean defaultEnabled = isProductionLike();
  if (!Boolean.parseBoolean(property("xianyu.security-guard.enabled", String.valueOf(defaultEnabled)))) {
      return;
  }
  ```

---

### 2.6 `.env.production.example` 环境变量名错误

- **文件**:[.env.production.example](file:///g:/源码/xianyu-assistant-package-temp/.env.production.example#L122)
- **行号**:第 122 行
- **问题描述**:
  `XIANYU_SCHEMA_RUNTIME_MUTATIONS_ENABLED=false`
  `application.yml` 第 174 行占位符是 `${SCHEMA_RUNTIME_MUTATIONS_ENABLED:true}`(无 `XIANYU_` 前缀),Spring Boot 只读取名为 `SCHEMA_RUNTIME_MUTATIONS_ENABLED` 的环境变量。`.env.production.example` 写的是 `XIANYU_SCHEMA_RUNTIME_MUTATIONS_ENABLED`,无法被识别,应用回退到默认值 `true`。

- **违反约束**:`database-migration-on-release.md` 规则五:"生产环境 SCHEMA_RUNTIME_MUTATIONS_ENABLED 必须保持 false"。
- **缓解因素**:`docker-compose.prod.yml` 第 99/140/169/197/218 行对 5 个服务硬编码了 `SCHEMA_RUNTIME_MUTATIONS_ENABLED: "false"`,Docker 部署不受影响;但非 Docker 部署(直接 JAR 启动)会中招。
- **严重程度**:高
- **建议修复**:改为 `SCHEMA_RUNTIME_MUTATIONS_ENABLED=false`(去掉 `XIANYU_` 前缀)。

---

### 2.7 automation-service V1.5 使用 TRUNCATE TABLE 清空会话表

- **文件**:[V1.5__rebuild_conversation.sql](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/migrations/V1.5__rebuild_conversation.sql#L15)
- **行号**:第 15 行
- **问题描述**:`TRUNCATE TABLE \`xianyu_conversation\`;` 直接违反 `database-migration-on-release.md` 规则二"严禁清空现有数据"和"严禁 TRUNCATE"。
  - 备份表名硬编码日期 `202506`,跨月执行后无法追溯
  - 第二次执行时 `IF NOT EXISTS` 跳过创建备份,TRUNCATE 再次清空但备份不刷新
  - migrations-manifest.json 第 594 行标记 `"risk": "rebuild"`,但 rebuild 策略与"严禁清空"规则冲突
- **严重程度**:高
- **建议修复**:已发布脚本不可改(`immutableAfterRelease: true`),但应:
  1. 在 manifest 中追加注释"严禁再次执行,已含 TRUNCATE"
  2. 后续类似场景改用 `DELETE FROM ... WHERE id > 0` 或 INSERT…SELECT + RENAME TABLE 交换方式

---

### 2.8 automation-service V1.25 使用 MySQL 8.0 不支持的 IF NOT EXISTS 语法

- **文件**:[V1.25__add_auto_rate_schedule_and_logs.sql](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/migrations/V1.25__add_auto_rate_schedule_and_logs.sql#L8)
- **行号**:第 8、11 行
- **问题描述**:
  ```sql
  ADD COLUMN IF NOT EXISTS `schedule_hour` ...      -- MySQL 8.0 不支持
  CREATE INDEX IF NOT EXISTS `idx_xyaarc_enabled_hour` ...  -- MySQL 8.0 不支持
  ```
  在 MySQL 8.0/8.4 上静默失败,导致列和索引未创建。`project_memory.md` 已记录该问题导致生产数据库缺失 schema 对象(索引 `xianyu_account_auto_rate_config.idx_xyaarc_enabled_hour`、表 `xianyu_auto_rate_log`、列 `card_group.sku_property_key`),引发后端服务启动失败。

- **严重程度**:高
- **建议修复**:新增 automation-service V1.26 修复脚本,参考 V1.64 和 V1.49 的 `INFORMATION_SCHEMA.COLUMNS` / `INFORMATION_SCHEMA.STATISTICS` + `PREPARE/EXECUTE` 动态 SQL 重建缺失的列和索引。

---

### 2.9 automation-service V1.4 / V1.6 不可重入迁移脚本

- **文件**:
  - [V1.4__add_conversation_fields.sql](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/migrations/V1.4__add_conversation_fields.sql#L13-L14) 第 13-14 行:`CREATE UNIQUE INDEX` 无 IF NOT EXISTS
  - [V1.6__fix_collation_consistency.sql](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/migrations/V1.6__fix_collation_consistency.sql#L16-L35) 第 16-35 行:`ALTER TABLE ... MODIFY` 改列排序规则
- **问题描述**:
  - V1.4 的 `ADD COLUMN` 和 `CREATE UNIQUE INDEX` 不可重入,重跑报错
  - V1.6 的 `MODIFY` 改变列字符集/排序规则,属 `database-migration-on-release.md` 规则四禁止的"破坏性 DDL",且不可重入
- **严重程度**:高(仅影响重跑场景)
- **建议修复**:已发布不可改,后续新增脚本必须遵循 V1.7/V1.49 的 `INFORMATION_SCHEMA` + `PREPARE/EXECUTE` 模式。

---

### 2.10 user-web App.vue 缺失 `xya-open-ai-cs` 事件监听器

- **文件**:[App.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/App.vue#L837-L872)
- **行号**:onMounted 第 837-858 行(缺失 addEventListener)vs onBeforeUnmount 第 872 行(有 removeEventListener)
- **问题描述**:
  App.vue 第 872 行 `window.removeEventListener('xya-open-ai-cs', openAiCs)` 移除了一个**从未注册**的事件监听器。`onMounted` 中没有对应的 `window.addEventListener('xya-open-ai-cs', openAiCs)`。

  但 `DashboardPage.vue` 第 890 行和 `DeliveryMallPage.vue` 第 772 行都通过 `window.dispatchEvent(new CustomEvent('xya-open-ai-cs'))` 派发该事件,期望 App.vue 监听并打开 AI 客服"小梦"面板。

- **影响**:Dashboard 和货源商城页面中触发"打开 AI 客服"的入口**完全失效**,用户点击后无任何响应。AI 客服面板目前只能通过 Topbar 按钮打开。
- **严重程度**:高
- **建议修复**:在 App.vue 的 `onMounted` 中补充:
  ```javascript
  window.addEventListener('xya-open-ai-cs', openAiCs)
  ```
  与第 872 行的 removeEventListener 配对。

---

## 三、中严重度问题(P1,尽快修复)

### 3.1 user-web 生产环境可通过直接 URL 访问数据同步页面

- **文件**:[App.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/App.vue#L180)
- **行号**:第 180 行(`'settings-sync': SyncSettings,` 在 pageMap 中无条件注册)
- **问题描述**:
  `allSettingKeys`(第 194-200 行)和 `nav.js`(第 105-111 行)都正确根据 `VITE_SHOW_DATA_SYNC === 'true'` 条件包含 settings-sync。但 `pageMap`(第 136-190 行)在第 180 行**无条件**注册了 `'settings-sync': SyncSettings`。

  `isKnownPage`(第 238 行)首先检查 `pageMap[path]`,因此即使生产环境 `allSettingKeys` 不包含 settings-sync,`isKnownPage('settings-sync')` 仍返回 `true`,用户在生产环境直接访问 `#/settings-sync` 即可进入数据同步页面。

- **违反约束**:`data-sync-bridge-token.md` 规则第五节"商业版前端不得显示数据同步板块"。
- **严重程度**:中
- **建议修复**:将 pageMap 中的 `'settings-sync'` 改为条件注册:
  ```javascript
  ...(import.meta.env.VITE_SHOW_DATA_SYNC === 'true' ? { 'settings-sync': SyncSettings } : {}),
  ```

---

### 3.2 user-web safeMediaUrl.js 信任检查被兜底架空

- **文件**:[safeMediaUrl.js](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/utils/safeMediaUrl.js#L174-L175)
- **行号**:第 174-175 行
- **问题描述**:
  `resolveAvatarUrl()` 前半部分(第 137-161 行)仔细检查了可信域名白名单(`avatarHosts`),但第 174-175 行的最终兜底:
  ```javascript
  // 最后兜底：只要是看起来正常的URL都尝试返回（头像展示风险低）
  return parsed.href
  ```
  对任何能通过 `new URL()` 解析的 http/https URL 直接返回,完全绕过前面的可信域名检查。
- **影响**:可信域名白名单形同虚设,可能导致用户头像加载来源不受控(隐私追踪像素、CDN 泄露等),与 `resolveTrustedMediaUrl` 的严格策略不一致。
- **严重程度**:中
- **建议修复**:移除最终兜底的 `return parsed.href`,对不在白名单的 URL 返回空字符串或默认头像。

---

### 3.3 crawler-service goofish.ts 辅助 fetch 调用违反硬约束

- **文件**:[goofish.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/goofish.ts#L519)
- **行号**:第 519 行(`mtopPost`)、第 558-571 行(`fetchExpectedStoreItemCount`)、第 989 行(调用点)
- **问题描述**:
  `fetchExpectedStoreItemCount` 使用 `fetch` 直接调用 MTOP API(`mtop.idle.web.user.page.head`)获取预期商品数量,违反硬约束"店铺商品爬取必须通过浏览器进行,不可使用 API 接口"。

  虽然主体爬取(第 999 行 `chromium.launch` + 第 1022-1047 行 MTOP 响应拦截)通过浏览器进行,但这个辅助 fetch 调用存在两个风险:
  1. 直接调用 MTOP API 可能触发 Baxia 风控,影响后续浏览器爬取成功率
  2. 严格解读硬约束,这个 fetch 调用违反了约束
- **严重程度**:中
- **建议修复**:移除 `fetchExpectedStoreItemCount` 调用,预期商品数量改为从浏览器拦截的 MTOP 响应中推断。

---

### 3.4 crawler-service sliderSolver.ts `attempt--` 可能导致循环次数超预期

- **文件**:[sliderSolver.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/sliderSolver.ts#L2316)
- **行号**:第 2316、2337、2421、2445 行
- **问题描述**:
  ```typescript
  if (await checkRefreshDialog(page)) {
    if (refreshRetryCount < MAX_REFRESH_RETRIES) {
      refreshRetryCount++;
      needReloadForRefresh = true;
      attempt--;  // ← 回退 attempt 计数
      continue;
    }
  }
  ```
  `attempt--` 让 while 循环计数器回退,刷新重试不消耗滑动配额。设计意图好,但累积风险:`MAX_REFRESH_RETRIES=5` + `MAX_DOWNLOAD_RETRIES=2` + `MAX_CLICK_RETRIES=5` + `maxRetries=5`,理论最大循环次数可能达到 17 次,单次求解可能耗时接近 170s 整体超时。
- **严重程度**:中
- **建议修复**:增加"总尝试次数"硬上限(如 `totalAttempts <= maxRetries * 3`),或在 `attempt--` 前检查 `attempt > 0` 避免负数。

---

### 3.5 crawler-service queue/index.ts BullMQ 任务重试次数偏少

- **文件**:[queue/index.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/queue/index.ts#L22-L30)
- **行号**:第 22-30 行
- **问题描述**:`attempts: 2`(只重试 1 次),对可能因 Baxia 风控、网络抖动、浏览器崩溃失败的爬取任务偏少。
- **严重程度**:中
- **建议修复**:增加到 `attempts: 3`,但需权衡 30s 间隔可能不够 Baxia 风控冷却。

---

### 3.6 admin-web useTable.ts AbortController 未传递给 apiFn

- **文件**:[useTable.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/hooks/core/useTable.ts#L345)
- **行号**:第 289-290、345 行
- **问题描述**:
  `fetchData` 创建了 `AbortController`,但调用 `apiFn(requestParams)` 时未将 `currentController.signal` 传递给 apiFn。导致:
  1. `cancelRequest` 调用 `abort()` 时,实际 HTTP 请求不会被取消(axios 未收到 signal)
  2. 第 348 行 `if (currentController.signal.aborted)` 检查永远为 false
  3. 组件卸载时正在进行的请求仍会完成并尝试更新已卸载组件状态
- **严重程度**:中
- **建议修复**:`apiFn({ ...requestParams, signal: currentController.signal })`,并确保各 API 函数将 signal 透传给 axios。

---

### 3.7 admin-web locales/index.ts 语言偏好存储位置不一致

- **文件**:[locales/index.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/locales/index.ts#L64)
- **行号**:第 64 行
- **问题描述**:
  `getDefaultLanguage` 第 64 行从 `localStorage` 读取,但 `useUserStore` 使用 `sessionStorage` 持久化(`storage: sessionStorage`)。用户设置语言后,页面刷新时从 localStorage 读不到,回退到默认 `LanguageEnum.ZH`。
- **严重程度**:中
- **建议修复**:改为 `sessionStorage.getItem(storageKey)`,或统一使用 `localStorage` 持久化语言偏好(语言偏好通常希望跨会话保留)。

---

### 3.8 admin-web auth/roles 指令 updated 钩子重复执行

- **文件**:
  - [auth.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/directives/core/auth.ts#L59-L62) 第 59-62 行
  - [roles.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/directives/core/roles.ts#L80-L83) 第 80-83 行
- **问题描述**:`authDirective` 和 `rolesDirective` 同时注册 `mounted` 和 `updated` 钩子。元素在 `mounted` 阶段因无权限被 `removeElement` 移除后,`updated` 钩子仍可能触发,重复执行权限检查逻辑,性能浪费。已移除的元素不会重新插入(指令是"移除"而非"隐藏"),`updated` 对已移除元素无意义。
- **严重程度**:中
- **建议修复**:仅在 `mounted` 钩子执行权限检查,或改用 `v-if` 方式实现响应式权限控制。

---

### 3.9 admin-web payment-config 强制标记已支付缺少二次验证

- **文件**:[payment-config/index.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/views/admin/payment-config/index.vue#L276-L296)
- **行号**:第 276-296 行
- **问题描述**:`forceMarkPaid` 仅需 `ElMessageBox.confirm` 点击"确认标记"即可执行。此操作涉及资金流(自动触发权益发放),缺少二次密码验证或操作日志记录。恶意管理员或被盗 session 的管理员可随意将订单标记为已支付。
- **严重程度**:中
- **建议修复**:增加二次密码验证,并在后端记录操作审计日志(`operationType: ORDER_FORCE_MARK_PAID`)。

---

### 3.10 admin-web ai-cs.ts 接口缺少 parent_id 和 entry_count 字段

- **文件**:[ai-cs.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/api/ai-cs.ts#L96-L99)
- **行号**:第 96-99 行
- **问题描述**:`AiCsKnowledgeCategory` 接口仅包含 `key` 和 `label`,缺少 `parent_id` 和 `entry_count` 字段。违反 `project_memory.md` 硬约束"AI客服知识库必须采用三级分类体系:一级分类(13个)包含二级分类(68个),二级分类包含知识库对话"。前端接口类型定义未体现层级关系。
- **严重程度**:中
- **建议修复**:扩展接口为 `{ key: string; label: string; parent_id?: number | null; entry_count?: number; children?: AiCsKnowledgeCategory[] }`。

---

### 3.11 admin-web learned-kb 分类管理未实现层级显示

- **文件**:[learned-kb/index.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/views/admin/learned-kb/index.vue#L529-L537)
- **行号**:第 529-537 行(`CategoryRow` 接口含 `parent_id`),第 179-224 行(`ElTable` 平铺展示)
- **问题描述**:`CategoryRow` 接口包含 `parent_id` 字段,但分类管理 Tab 的 `ElTable` 直接平铺展示所有分类,未根据 `parent_id` 实现树形/层级显示,没有展示父分类信息。
- **严重程度**:中
- **建议修复**:使用 `ElTable` 的 `row-key` 和 `tree-props` 配置实现树形表格,或在前端将平铺列表转换为树形结构后渲染。

---

### 3.12 admin-web HTTP 拦截器双重序列化风险

- **文件**:[http/index.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/utils/http/index.ts#L166-L169)
- **行号**:第 166-169 行
- **问题描述**:请求拦截器中 `request.data = JSON.stringify(request.data)` 手动序列化,但 axios 默认 `transformRequest` 也会对对象进行 JSON 序列化。如果调用方传入的 `request.data` 已经是字符串,会导致双重序列化。`FormData` 已排除,但 `URLSearchParams`、`Blob`、`ArrayBuffer` 未排除。
- **严重程度**:中
- **建议修复**:依赖 axios 默认 `transformRequest`,或仅在没有设置 `Content-Type` 且数据为普通对象时才手动序列化。

---

### 3.13 admin-web sanitizeHtml SAFE_URL_PATTERN 未阻止 javascript: 协议变体

- **文件**:[sanitizeHtml.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/utils/sanitizeHtml.ts#L6)
- **行号**:第 6 行
- **问题描述**:`SAFE_URL_PATTERN` 为 `/^(https?:|mailto:|tel:|data:image\/...|\/|#)/i`,未显式阻止 `javascript:` 协议。虽然 `on*` 属性已被移除,但如果存在未覆盖的属性允许 URL 值,`javascript:` 可能绕过。`data:text/html` 等危险 data URI 未被完全阻止。
- **严重程度**:中
- **建议修复**:在 URL 属性检查中显式拒绝 `javascript:`、`vbscript:`、`data:text/html` 等危险协议,采用白名单而非正则匹配。

---

### 3.14 admin-web vite.config.ts manualChunks vue 匹配过宽

- **文件**:[vite.config.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/vite.config.ts#L165)
- **行号**:第 165 行
- **问题描述**:`if (id.includes('vue') || id.includes('pinia') || id.includes('vue-router')) return 'vendor-vue'`。`id.includes('vue')` 会匹配所有路径中包含 `vue` 的模块,如 `vue-echarts`、`vue-i18n` 等,这些库体积较大且与核心 vue 运行时无关,被打包到 `vendor-vue` chunk 会导致该 chunk 过大,影响首屏加载。
- **严重程度**:中
- **建议修复**:使用更精确的匹配,如 `id.includes('/node_modules/vue/')` 或 `id.includes('/node_modules/@vue/')`,单独拆分 `vue-echarts`、`vue-i18n` 等库。

---

### 3.15 admin-web captcha-records watch 与 onMounted 重复触发

- **文件**:[captcha-records/index.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/views/admin/captcha-records/index.vue#L1099-L1122)
- **行号**:第 1099-1116 行(watch immediate)、第 1119-1122 行(onMounted)
- **问题描述**:`watch(() => route.query.userId, ..., { immediate: true })` 在组件挂载时立即执行,触发 `loadStats`、`loadQueueStatus`、`loadList`。同时 `onMounted` 又调用了 `loadQueueStatus`。导致 `loadQueueStatus` 在初始化时被调用两次。
- **严重程度**:中
- **建议修复**:移除 `onMounted` 中的 `loadQueueStatus()` 调用(watch immediate 已触发),仅保留 `setInterval`。

---

### 3.16 admin-web recharge-records watch immediate 与 onSearch 重复加载

- **文件**:[recharge-records/index.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/views/admin/recharge-records/index.vue#L426-L434)
- **行号**:第 426-434 行
- **问题描述**:`watch(() => route.query.userId, ..., { immediate: true })` 触发 `loadList`。用户通过搜索栏输入后点击查询,`onSearch` 调用 `syncUserIdToRoute` 更新路由 query,再次触发 watch,导致 `loadList` 被调用两次。watch immediate 触发时,`userIdInput.value` 会被路由值覆盖。
- **严重程度**:中
- **建议修复**:在 watch 中区分"路由主动变化"和"搜索栏触发的路由变化",添加标志位 `isSyncingRoute` 区分。

---

### 3.17 admin-web ai-pricing 三档定价加载失败时仍允许保存

- **文件**:[ai-pricing/index.vue](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/views/admin/ai-pricing/index.vue#L68)
- **行号**:第 68 行
- **问题描述**:`onTierSave` 按钮的 `:loading="tierSaving"` 仅在保存时禁用。当 `tierState === 'error'`(加载失败)时,表单区域被 `v-else` 隐藏,但保存按钮仍在 header 中,用户可在配置加载失败的情况下点击保存,可能将默认值或空值覆盖到线上。
- **严重程度**:中
- **建议修复**:在保存按钮上增加 `:disabled="tierState !== 'ready'"` 条件。

---

### 3.18 V1.49 三级分类 INSERT 不可重入

- **文件**:[V1.49__three_level_kb_categories.sql](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/resources/db/migration/V1.49__three_level_kb_categories.sql#L74)
- **行号**:第 74 行(13 个一级分类 INSERT)、第 132/145/157/170 行(68 个二级分类 INSERT)
- **问题描述**:`ai_cs_kb_category` 表有唯一键 `uk_kb_cat_name_hash (name_hash, deleted)`。V1.49 用 `name_hash = MD5(name)` 插入,重跑时 name_hash 相同、deleted=0 相同,触发 `Duplicate entry` 错误。第 70 行先软删现有分类,但新插入的分类在重跑时也会被软删,幂等性不完整。
- **严重程度**:中
- **建议修复**:INSERT 改为 `INSERT IGNORE` 或 `INSERT ... ON DUPLICATE KEY UPDATE updated_time = NOW()`。

---

### 3.19 V1.50-V1.62 种子 Q&A INSERT 不可重入

- **文件**:[V1.50__seed_qa_general_trade.sql](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/resources/db/migration/V1.50__seed_qa_general_trade.sql) 等 13 个文件
- **问题描述**:`ai_cs_learned_kb` 表有唯一键 `uk_learned_kb_hash (content_hash, deleted)`。种子脚本用 `content_hash = MD5(CONCAT(question, answer))` 插入,重跑时触发唯一键冲突。13 个种子脚本共约 390 条 Q&A,重跑会全部失败。
- **严重程度**:中
- **建议修复**:INSERT 改为 `INSERT IGNORE`,或在 SELECT 子查询中加 `WHERE NOT EXISTS` 过滤。

---

### 3.20 .env MYSQL_ROOT_PASSWORD 与 application.yml 不一致

- **文件**:[.env](file:///g:/源码/xianyu-assistant-package-temp/.env#L1) 第 1 行
- **问题描述**:`.env` 第 1 行 `MYSQL_ROOT_PASSWORD=root`,但 `application.yml` 第 59 行硬编码 `password: 123456`(与 `local-dev-no-docker.md` 规则一致)。`.env` 的 `root` 是 Docker 模式下的密码,本地开发不依赖 Docker 时这个变量实际不被 application.yml 读取,但会造成认知混乱。
- **严重程度**:中
- **建议修复**:将 `.env` 第 1 行改为 `MYSQL_ROOT_PASSWORD=123456` 与 application.yml 对齐。

---

### 3.21 Nginx 未启用 Brotli 压缩和 HTTP/2 Push

- **文件**:[us-nginx-full.conf](file:///g:/源码/xianyu-assistant-package-temp/deploy/nginx/us-nginx-full.conf#L21-L35)
- **问题描述**:
  - 项目 memory 要求"Nginx 需启用 Brotli 压缩",实际仅用 `gzip_static on` + `gzip` 指令,未加载 `ngx_brotli` 模块,未配置 `brotli on` / `brotli_static on`。前端构建产物只生成 `.gz` 文件,未生成 `.br` 文件。
  - 项目 memory 要求"使用 HTTP/2 推送关键资源",配置中无 `http2_push` 指令,也未启用 `listen 443 ssl http2`。(注:HTTP/2 Push 已被 Chrome 弃用,现代实践推荐 `<link rel="preload">`)
- **严重程度**:中
- **建议修复**:Nginx 加载 brotli 模块并配置;前端 vite 增加 `vite-plugin-compression` 的 `brotli: true` 选项。HTTP/2 Push 改为前端 `<link rel="preload">` 预加载关键资源。

---

## 四、低严重度问题(P2,计划修复)

### 4.1 core-api 退款接口参数差异 Java 端未校验

- **文件**:[AutomationProxyController.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/controller/AutomationProxyController.java#L2215-L2280)
- Java 网关仅透传到 Python,三个接口的参数差异(type/data/valueType)完全由 Python 端处理。建议在 Java 网关层增加参数白名单校验作为防御性编程。

### 4.2 crawler-service sliderSolver.ts 接口注释 maxRetries 默认值错误

- **文件**:[sliderSolver.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/sliderSolver.ts#L37) 第 37 行
- 注释写「默认 3」,实际默认值是 5(第 1724 行)。代码逻辑正确,仅注释错误。

### 4.3 crawler-service db/index.ts 连接池 max=10 可能偏小

- **文件**:[db/index.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/db/index.ts#L90-L95) 第 90-95 行
- 高并发场景可能成为瓶颈。建议提升到 `max: 15` 或 `max: 20`,可通过环境变量配置。

### 4.4 crawler-service migrate.ts REVIEWED_MANIFEST_SHA256 硬编码

- **文件**:[migrate.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/migrate.ts#L4) 第 4 行
- 设计上的安全措施,但迁移脚本更新后忘记同步更新此 sha256 会导致生产环境迁移失败。建议在 CI/CD 流水线中添加校验步骤。

### 4.5 user-web quickReply.js 注释编码损坏(乱码)

- **文件**:[quickReply.js](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/api/quickReply.js#L7) 第 7、36、45 行
- 注释出现 mojibake 乱码(如"蹚嵎鍥炲妯℃澘 CRUD"应为"快捷回复模板 CRUD")。不影响运行,但影响可读性。建议用 UTF-8 重新写入。

### 4.6 admin-web 多个低严重度问题

| 问题 | 文件 | 行号 |
|------|------|------|
| 登录失败后未重置滑块验证状态 | login/index.vue | 197-200 |
| searchHistory 登出时未清空 | store/modules/user.ts | 146-190 |
| useChart styleCache 未考虑 show 参数 | useChart.ts | 216-227 |
| 路由配置使用 `as any` 绕过类型检查 | router/modules/admin.ts | 多处 |
| useTable pending 请求兜底清理定时器未实现 | http/index.ts | 44、70 |
| sanitizeHtml SSR 环境仅移除 script 标签 | sanitizeHtml.ts | 10-12 |
| lock-verifier ITERATIONS 固定无升级机制 | lock-verifier.ts | 1-2 |
| UserInfo 接口 buttons/roles 必填可能导致报错 | types/api/api.d.ts | 78-85 |
| 公告 currentAnnouncement 未考虑排序 | announcement/index.vue | 158 |
| carousel 保存后先 Object.assign 再 loadConfig 闪烁 | carousel/index.vue | 371-373 |
| captcha-records usernameHint 未正确赋值 | captcha-records/index.vue | 980-982 |
| recharge-records loadSummary 失败时静默 | recharge-records/index.vue | 348-377 |
| payment-config 表单缺少必要字段验证 | payment-config/index.vue | 101-115 |
| audit-logs CSV 导出未限制最大行数 | audit-logs/index.vue | 138-151 |
| client-errors 未提供错误堆栈查看 | client-errors/index.vue | 51-71 |

### 4.7 数据库迁移脚本低严重度问题

| 问题 | 文件 | 行号 |
|------|------|------|
| V1.31 物理删除软删记录 | V1.31__delivery_goods_config_unique_constraint.sql | 26、29-39 |
| V1.25/V1.26/V1.13 ADD COLUMN 不可重入 | 多个文件 | - |
| V1.65 单行配置表无默认行插入 | V1.65__create_growth_partner_tables.sql | 19-32 |
| .mvn/jvm.config 构建堆仅 512m | .mvn/jvm.config | 1 |
| .env.example 缺少 VITE_TERMS_URL 占位值 | user-web/.env.example | 2-3 |
| application.yml 硬编码数据库密码未参数化 | application.yml | 58-59 |
| application.yml 默认 token 含占位符弱值 | application.yml | 153、178、182、203、209 |

---

## 五、已验证符合约束的关键点

以下模块经审查**符合工作区规则和硬约束**,无 Bug:

### 5.1 core-api Java 后端

| 模块 | 验证文件 | 结论 |
|------|---------|------|
| AiBillingService 计费逻辑 | AiBillingService.java 第 527-542 行 | precheck 第 542 行抛 402 兜底正确;resolveTierTokensPerCall 按 VIP 等级查询 |
| 按次计费强制逻辑 | AiBillingService.java | `module_key == "model-config-general"` 强制 `billingMode = "per_call"` |
| AuthService 种子初始化 | AuthService.java 第 224 行 | `if (!seedEnabled || isProdProfile())` 仅非生产环境执行 |
| UserAuthService 种子用户 | UserAuthService.java 第 328 行 | 同上,且仅 sys_user 表为空时执行 |
| SyncAuthFilter 常量时间比较 | SyncAuthFilter.java 第 62-67 行 | 使用 `MessageDigest.isEqual` 常量时间比较;fail-closed |
| setting_key 区分 | BusinessSettingsService.java 第 31-36 行 | `ai-customer-service` 与 `xiaomeng-assistant` 独立 |
| 知识库三级分类 | V1.49 + AiCsLearnedKbService.java | 13 一级 + 68 二级分类;listCategoriesForUser 返回三级树形结构 |
| SchemaCompatibilityRunner 幂等创建 | SchemaCompatibilityRunner.java | addColumnIfMissing 用 INFORMATION_SCHEMA;createTable 用 IF NOT EXISTS |
| multipart 文件上传限制 | application.yml 第 23-25 行 | `max-file-size: 20MB, max-request-size: 50MB` |
| AutomationClient 拆包 ResultObject | AutomationClient.java 第 41、575-582 行 | `findAndRegisterModules()` 正确;dataOrSelf 提取 data 字段 |
| orderId/refundId 字符串类型 | AutomationProxyController.java 第 2196-2219 行 | `@PathVariable String refundId` 保持字符串 |
| Token 一致性 | BusinessSettingsService.java 第 256 行 + application.yml 第 195 行 | 两处 token 完全一致:`HIDpsuvrKSlWfczLiFTJa0Ydhqm8gx7Q` |
| HikariCP 连接池 | application.yml 第 60-78 行 | `maximum-pool-size: 50, minimum-idle: 20, connection-timeout: 3000` |

### 5.2 automation-service Python

| 模块 | 验证文件 | 结论 |
|------|---------|------|
| 滑块求解错误分类 | captcha_solver.py 第 351-414 行 | timeout/browser_crashed/service_unavailable 三类区分正确 |
| captcha_queue MAX_RETRY_BY_REASON | captcha_queue.py 第 68-81 行 | `{slider_fail:3, timeout:1, browser_crashed:1}` 正确 |
| 商品搜索三种模式 | misc.py `_execute_search_with_mode` | fast/slow/auto 模式正确,Baxia 风控检测完整 |
| MTOP 签名 | xianyu_goods_sync.py | `_xianyu_mtop_request` 使用 `_m_h5_tk` token 部分生成 MD5 签名 |
| 搜索字段映射 | misc.py `_call_crawler_search` | itemId/title/price/imageUrl/userNickName/area 标准化正确 |

### 5.3 crawler-service Node

| 模块 | 验证文件 | 结论 |
|------|---------|------|
| RISK_COOKIE_NAMES 完整性 | sliderSolver.ts 第 317-327 行 | 包含全部 9 个字段(x5secdata/x5sec/x5sectag/x5pref/bx-cookie-test/tfstk/cbc/sca/isg) |
| 刷新重试前清除 risk cookies | sliderSolver.ts 第 2153-2173 行 | 用 `context.cookies()` 获取并过滤后重新注入 |
| isPunishUrl 检测 | sliderSolver.ts 第 365-369 行 | 包含 `_____tmd_____/punish` |
| SOLVE_OVERALL_TIMEOUT_MS | server.ts 第 813、834 行 | `170000` Promise.race 兜底正确 |
| BrowserSlot 5 分钟超时 | server.ts 第 131-139 行 | 强制释放槽位 |
| SEARCH_API_MARKER 精确匹配 | goofishSearch.ts 第 38 行 | `mtop.taobao.idlemtopsearch.pc.search` |
| Promise.race 事件驱动 | goofishSearch.ts 第 760-766 行 | MTOP 响应 vs 6 秒超时竞速 |
| Cookie 注入 storageState | goofishSearch.ts 第 622-646 行 | domain 为 `.goofish.com` |
| 源码与编译产物同步 | goofishSearch.ts vs goofishSearch.js | 逻辑一致 |

### 5.4 user-web 前端

| 模块 | 验证文件 | 结论 |
|------|---------|------|
| LoginPage DEV 预填 | LoginPage.vue 第 379-380 行 | `import.meta.env.DEV ? 'admin' : ''` 正确 |
| ProductPublishPage Token 校验 | ProductPublishPage.vue 第 1305 行 | `ensureAiTokenBalance()` 在 AI 请求前调用 |
| OpportunityPage Token 校验 | OpportunityPage.vue 第 1309 行 | `ensureAiTokenBalance()` 在改写前调用 |
| WorkflowPage Token 校验 | WorkflowPage.vue 第 1564 行 | `ensureAiTokenBalance()` 在 testPolish 前 |
| AiCsSettings Token 校验 | AiCsSettings.vue 第 486 行 | `ensureAiTokenBalance()` 在 runTest 前 |
| aiTokenGuard.js 实现 | aiTokenGuard.js 第 22、29、53-56 行 | 余额<=0 拦截;查询失败不阻断 |
| nav.js settingsTabs 控制 | nav.js 第 105-111 行 | `VITE_SHOW_DATA_SYNC === 'true'` 条件 |
| .env / .env.production | .env 第 9 行 / .env.production 第 3 行 | `VITE_SHOW_DATA_SYNC` 本地 true,生产 false |
| request.js baseURL | request.js 第 42 行 | `baseURL: '/api'`,无双重前缀 |
| 前端去重通知 | AiCsPanel.vue 第 371-393 行 | localStorage 按日记录,跨会话仅提醒一次 |
| persistentCache SWR 模式 | persistentCache.js | freshMs/staleMs 双阈值,降级正确 |
| errorReporter 脱敏 | errorReporter.js | Bearer/JWT/邮箱/手机号/password/cookie/secret 脱敏完整 |
| sse.js 连接管理 | sse.js | ticket 鉴权、指数退避、最大重连 20、keepAlive 90s |
| Vite 构建优化 | vite.config.js | manualChunks 拆分 vendor、cssCodeSplit、defineAsyncComponent 懒加载 |

### 5.5 admin-web 前端

| 模块 | 验证文件 | 结论 |
|------|---------|------|
| 登录页 DEV 预填 | login/index.vue 第 134-137 行 | `import.meta.env.DEV` 判断正确 |
| 模型配置通用模型字段 | model-config/index.vue 第 48-126 行 | 字段清单符合规则,已移除字段未恢复 |
| 三档定价卡片 | ai-pricing/index.vue 第 60-126 行 | 普通/VIP/SVP 三档 + ElInputNumber |
| TierPriceConfig 接口 | billing.ts 第 81-99 行 | moduleKey/normal/vip/svp 完整 |

### 5.6 数据库迁移和配置

| 模块 | 验证文件 | 结论 |
|------|---------|------|
| 迁移清单完整性 | db/migrations-manifest.json | immutableAfterRelease/requireContiguousVersions/sha256 全部符合 |
| Token 一致性 | BusinessSettingsService.java + application.yml | 两处 token 完全一致 |
| HikariCP 连接池 | application.yml 第 60-78 行 | `maximum-pool-size: 50, minimum-idle: 20, connection-timeout: 3000` |
| multipart 限制 | application.yml 第 23-25 行 | `max-file-size: 20MB, max-request-size: 50MB` |
| 数据卷持久化 | docker-compose.yml 第 383-387 行 | `mysql_data`、`crawler_pg_data` 等命名卷正确 |
| 生产种子账号关闭 | docker-compose.prod.yml 第 96 行 | `ADMIN_SEED_ENABLED: "false"` 硬编码 |
| VITE_SHOW_DATA_SYNC 开关 | user-web/.env + .env.production | 本地 true,生产 false |
| JVM 运行时配置 | docker-compose.prod.yml 第 110 行 | `MaxRAMPercentage=75 + UseG1GC + MaxGCPauseMillis=200` |
| Redis 配置 | docker-compose.prod.yml 第 51 行 | `appendonly no, save 300 10, maxmemory 1gb, allkeys-lru` |
| MySQL 生产调优 | docker-compose.prod.yml 第 27 行 | `innodb-buffer-pool-size=8G, innodb-log-file-size=1G, flush-log-at-trx-commit=2` |
| V1.64 索引修复 | V1.64__fix_silent_failed_indexes.sql | 修复 V1.11 静默失败的 5 个索引 |
| prod_deploy.py 迁移门控 | prod_deploy.py 第 1641、1666-1670 行 | `--migration-evidence` sha256 校验 |

---

## 六、修复优先级建议

### P0(立即修复,事故级风险)

| # | 问题 | 文件 | 修复动作 |
|---|------|------|---------|
| 1 | _auto_confirm_shipment 同步阻塞 | ws_delivery_handler.py 第 2611 行 | 改用 `await asyncio.to_thread(...)` |
| 2 | order.py 路由同步阻塞 | order.py 第 112、146 行 | 同上 |
| 3 | server.ts timeoutMs 默认 90000 | server.ts 第 840 行 | 改为 30000 |
| 4 | server.ts maxRetries 上限 8 | server.ts 第 839 行 | 改为 5 |
| 5 | 37 处 ObjectMapper 未注册 JavaTimeModule | core-api 多处 | 创建全局 JacksonConfig Bean |
| 6 | DeliveryExecutionService 去重不完整 | DeliveryExecutionService.java 第 153-168 行 | 增加 status=3 1小时窗口 + order_status=3 强校验 |
| 7 | StartupSecurityGuard 默认 opt-in | StartupSecurityGuard.java 第 39-41 行 | 生产环境默认启用 |
| 8 | .env.production.example 变量名错误 | .env.production.example 第 122 行 | 去掉 `XIANYU_` 前缀 |
| 9 | automation-service V1.25 IF NOT EXISTS | V1.25__add_auto_rate_schedule_and_logs.sql | 新增 V1.26 修复脚本 |
| 10 | App.vue 缺失 xya-open-ai-cs 监听 | App.vue onMounted | 补充 addEventListener |

### P1(尽快修复,功能缺陷或安全隐患)

| # | 问题 | 文件 | 修复动作 |
|---|------|------|---------|
| 11 | 生产环境可访问 settings-sync | App.vue 第 180 行 | pageMap 条件注册 |
| 12 | resolveAvatarUrl 信任检查被架空 | safeMediaUrl.js 第 174-175 行 | 移除兜底 return |
| 13 | goofish.ts 辅助 fetch 违反约束 | goofish.ts 第 519、989 行 | 移除 fetchExpectedStoreItemCount |
| 14 | sliderSolver attempt-- 累积风险 | sliderSolver.ts 第 2316 行 | 增加总尝试次数上限 |
| 15 | useTable AbortController 未生效 | useTable.ts 第 345 行 | 传递 signal 给 apiFn |
| 16 | locales 语言偏好存储不一致 | locales/index.ts 第 64 行 | 改为 sessionStorage 或统一 localStorage |
| 17 | auth/roles 指令 updated 重复执行 | auth.ts / roles.ts | 仅 mounted 执行 |
| 18 | 强制标记已支付缺二次验证 | payment-config/index.vue 第 276-296 行 | 增加密码验证 + 审计日志 |
| 19 | AiCsKnowledgeCategory 接口缺字段 | ai-cs.ts 第 96-99 行 | 扩展 parent_id/entry_count/children |
| 20 | learned-kb 分类未层级显示 | learned-kb/index.vue 第 179-224 行 | 用 ElTable tree-props |
| 21 | V1.49 INSERT 不可重入 | V1.49__three_level_kb_categories.sql | 改为 INSERT IGNORE |
| 22 | V1.50-V1.62 种子 INSERT 不可重入 | 13 个种子脚本 | 改为 INSERT IGNORE |
| 23 | Nginx 未启用 Brotli | us-nginx-full.conf | 加载 brotli 模块 |
| 24 | .env MYSQL_ROOT_PASSWORD 不一致 | .env 第 1 行 | 改为 123456 |

### P2(计划修复,体验/性能/可维护性)

其余 17 个低严重度问题,按模块自行排期修复,不阻塞上线。

---

## 七、附录

### 7.1 检测方法说明

本次检测采用并行子代理分模块审查模式,共启动 6 个 search 子代理并行分析:

1. core-api Java 后端(计费/自动发货/数据同步/退款/AI客服/鉴权/迁移)
2. automation-service Python(滑块求解/自动发货/商品搜索/MTOP/小梦客服)
3. crawler-service Node(滑块求解/商品搜索/服务器配置/数据库/队列)
4. user-web 前端(登录/商品发布/商机发掘/工作流/AI客服/路由/性能)
5. admin-web 前端(登录/模型配置/费用设置/知识库/权限/HTTP工具/安全)
6. 数据库迁移和配置(迁移脚本/清单/配置文件/Docker/Nginx/Token一致性)

每个子代理独立读取代码并对照工作区规则(.trae/rules)和 project_memory.md 硬约束进行验证,最后汇总成本报告。

### 7.2 规则一致性校验结果

| 规则文件 | 校验结果 |
|---------|---------|
| data-sync-bridge-token.md | ✅ Token 一致;⚠️ 生产环境可通过 URL 访问 settings-sync(问题 3.1) |
| database-migration-on-release.md | ❌ V1.5 TRUNCATE;❌ V1.25 IF NOT EXISTS 静默失败;❌ V1.4/V1.6 不可重入 |
| general-model-per-call-billing.md | ✅ 按次计费强制逻辑正确;✅ 三档定价配置正确;✅ 前端 Token 校验完整 |
| goofish-keyword-search.md | ✅ 三种模式正确;✅ MTOP API 端点正确;✅ searchMode 返回字段正确 |
| local-dev-default-credentials.md | ✅ 种子初始化仅非生产;✅ DEV 模式预填正确;✅ 生产 ADMIN_SEED_ENABLED=false |
| local-dev-no-docker.md | ✅ 数据库连接凭据正确;⚠️ .env MYSQL_ROOT_PASSWORD 不一致(问题 3.20) |

### 7.3 相关文件清单(按模块)

**core-api**:
- `apps/core-api/src/main/java/com/xianyu/admin/service/DeliveryExecutionService.java`
- `apps/core-api/src/main/java/com/xianyu/admin/service/SyncReceiveService.java`
- `apps/core-api/src/main/java/com/xianyu/admin/service/AiCsService.java`
- `apps/core-api/src/main/java/com/xianyu/admin/service/AiBillingService.java`
- `apps/core-api/src/main/java/com/xianyu/admin/controller/AutomationProxyController.java`
- `apps/core-api/src/main/java/com/xianyu/admin/service/BusinessSettingsService.java`
- `apps/core-api/src/main/java/com/xianyu/admin/config/StartupSecurityGuard.java`
- `apps/core-api/src/main/resources/db/migration/V1.49__three_level_kb_categories.sql`

**automation-service**:
- `apps/automation-service/app/services/ws_delivery_handler.py`
- `apps/automation-service/app/api/v1/routes/order.py`
- `apps/automation-service/app/services/captcha_solver.py`
- `apps/automation-service/migrations/V1.5__rebuild_conversation.sql`
- `apps/automation-service/migrations/V1.25__add_auto_rate_schedule_and_logs.sql`

**crawler-service**:
- `apps/crawler-service/src/server.ts`
- `apps/crawler-service/src/crawler/sliderSolver.ts`
- `apps/crawler-service/src/crawler/goofish.ts`
- `apps/crawler-service/src/queue/index.ts`

**user-web**:
- `apps/user-web/src/App.vue`
- `apps/user-web/src/utils/safeMediaUrl.js`
- `apps/user-web/src/api/quickReply.js`

**admin-web**:
- `apps/admin-web/src/hooks/core/useTable.ts`
- `apps/admin-web/src/locales/index.ts`
- `apps/admin-web/src/directives/core/auth.ts`
- `apps/admin-web/src/directives/core/roles.ts`
- `apps/admin-web/src/views/admin/payment-config/index.vue`
- `apps/admin-web/src/api/ai-cs.ts`
- `apps/admin-web/src/views/admin/learned-kb/index.vue`
- `apps/admin-web/src/utils/http/index.ts`
- `apps/admin-web/src/utils/sanitizeHtml.ts`
- `apps/admin-web/vite.config.ts`

**配置和部署**:
- `.env.production.example`
- `.env`
- `deploy/nginx/us-nginx-full.conf`
- `apps/core-api/src/main/resources/application.yml`

---

**报告生成时间**:2026-07-31
**检测覆盖范围**:core-api / automation-service / crawler-service / user-web / admin-web / 数据库迁移 / 部署配置
**问题总数**:48(高 10 / 中 21 / 低 17)
**建议优先修复**:P0 10 个、P1 14 个、P2 17 个按计划修复
