# 项目全量扫描与浏览器测试报告

> 扫描时间：2026-07-17
> 扫描范围：core-api (Java)、automation-service (Python)、crawler-service (Node/TS)、user-web (Vue3)、admin-web (Vue3+TS)、根目录工程卫生
> 测试方式：静态代码扫描 + 浏览器自动化（user-web@5174 / admin-web@3006 / core-api@18080 / automation@12401 / crawler@3001 全部运行中）
> 本轮**仅产出报告，未改动任何代码**

---

## 目录

- [一、严重问题（事故级，建议优先修复）](#一严重问题事故级建议优先修复)
- [二、中等问题（潜在 Bug 与设计缺陷）](#二中等问题潜在-bug-与设计缺陷)
- [三、轻微问题（代码异味与可维护性）](#三轻微问题代码异味与可维护性)
- [四、浏览器测试发现的问题](#四浏览器测试发现的问题)
- [五、工程卫生问题](#五工程卫生问题)
- [六、合规性确认（已做对的部分）](#六合规性确认已做对的部分)
- [七、优先修复建议路线图](#七优先修复建议路线图)

---

## 一、严重问题（事故级，建议优先修复）

### S1. 后端 JSON 响应头缺 `charset=utf-8`，非浏览器客户端中文乱码

- **位置**：[apps/core-api/src/main/java/com/xianyu/admin/config/ResultHttpStatusAdvice.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/config/ResultHttpStatusAdvice.java)、[application.yml](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/resources/application.yml)
- **现象**：`GET /api/health` 与 `GET /api/login/capabilities` 实测响应头 `Content-Type: application/json`（无 charset），body 中文按 ISO-8859-1 解码后为 `æä½æå`（应为"操作成功"）。
- **影响**：浏览器 axios 默认按 UTF-8 解码所以前端正常，但 curl/Postman/Invoke-WebRequest/运维脚本/监控探针/合作伙伴对接时中文全部乱码，可能误判后端故障。
- **建议**：在 `WebConfig.java` 或 `application.yml` 中强制 `spring.http.encoding.charset=UTF-8`、`force=true`；或在 `ResultHttpStatusAdvice.beforeBodyWrite` 中显式 `response.getHeaders().setContentType(MediaType.APPLICATION_JSON_UTF8)`。

### S2. AuthService 登录失败分支未消费密码校验令牌，存在时序攻击

- **位置**：[apps/core-api/src/main/java/com/xianyu/admin/service/AuthService.java:86-88](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/AuthService.java)
- **现象**：`if (!verifyPassword(password, storedHash))` 失败分支**未调用** `consumePasswordVerification(password)`，而其他失败分支（用户不存在/禁用）都调用了。攻击者可通过响应时间区分"用户不存在"与"密码错误"，进而枚举有效账号。
- **建议**：在第 87 行 throw 之前补充 `consumePasswordVerification(password)`，确保所有失败路径耗时一致。

### S3. 安全门禁默认关闭 + 默认弱密钥兜底

- **位置**：
  - [StartupSecurityGuard.java:39-41](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/config/StartupSecurityGuard.java)：`xianyu.security-guard.enabled` 默认 `false`
  - [AutomationClient.java:33,523-529](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/AutomationClient.java)：`DEV_INTERNAL_TOKEN = "dev-only-internal-api-token-change-me-32-chars"` 兜底
  - [AutomationProxyController.java:63-64](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/controller/AutomationProxyController.java)：`@Value("${xianyu.cookie.crypto-secret:dev-only-cookie-crypto-secret-change-me-32-chars}")`
  - [application.yml:108,133,137](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/resources/application.yml)：JWT secret、cookie crypto、internal token 均带弱默认值
- **影响**：若生产环境变量注入失败，应用仍会以弱密钥启动并继续处理请求，闲鱼账号 Cookie 加密形同虚设，内部服务间 token 可被伪造。
- **建议**：在 `prod` profile 下强制 `security-guard.enabled=true`；删除所有 `dev-only-*-change-me` 默认值，缺失即抛 `IllegalStateException` 终止启动。

### S4. 事务方法内调用远程 HTTP，长事务占连接池

- **位置**：[DeliveryExecutionService.java:115,333,454](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/DeliveryExecutionService.java)
- **现象**：`@Transactional public void executeDelivery(...)` 内部调用 `automationClient.postInternalForData()`（最长 30s 超时）。HikariCP `maximum-pool-size=10`，高并发待发货场景下连接池会迅速耗尽。
- **建议**：将 HTTP 调用拆出事务边界，事务内只做状态变更与日志，HTTP 调用前后通过状态机驱动（如 `pending → calling → done/failed`）。

### S5. DataSyncService SSRF：用户输入 URL 未校验

- **位置**：[DataSyncService.java:105-119](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/DataSyncService.java) `pingRemote()`
- **现象**：直接拿前端传入的 `targetBaseUrl` 拼接 URL 发起 HTTP 请求，未走 host allowlist 或私网 IP 拦截。
- **影响**：攻击者可探测内网（`http://192.168.x.x`、`http://169.254.169.254` 云元数据等）。
- **建议**：复用 `OutboundImageUrlPolicy` 类似的 host 白名单 + 私网/保留 IP 段拦截。

### S6. sliderSolver 持久化浏览器 userDataDir 完全未清理

- **位置**：[crawler-service/src/crawler/sliderSolver.ts:1573-1604, 2139-2145](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/sliderSolver.ts)
- **现象**：每次滑块验证都创建 `chrome-slider-warm-${Date.now()}` 目录启动 Chrome，`finally` 块只关闭 context/browser，**未 `fs.rm(userDataDir, { recursive: true, force: true })`**。
- **影响**：(a) 高频滑块场景磁盘快速被占满；(b) **用户闲鱼 Cookie/localStorage 持久残留磁盘**，容器重启后仍可被后续请求读取，存在会话泄露风险。
- **建议**：finally 末尾 `await fs.rm(userDataDir, { recursive: true, force: true }).catch(() => {})`。

### S7. crawler-service 全局未捕获异常处理器完全缺失

- **位置**：[crawler-service/src/server.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/server.ts)、[crawler-service/src/worker.ts](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/worker.ts)
- **现象**：两个进程入口均未注册 `process.on('unhandledRejection', ...)` / `process.on('uncaughtException', ...)`。代码中大量 `void xxx().catch(...)` 与 `Promise.allSettled`，但 Playwright 异步回调（如 `page.on('response', async ...)`）若抛出未捕获异常，Node 15+ 默认会让进程退出。
- **建议**：在 `start()` 顶部注册全局处理器并记录日志；为关键异步路径补齐 try/catch。

### S8. user-web WorkflowPage 所有 AI 操作反馈静默失效

- **位置**：[apps/user-web/src/pages/WorkflowPage.vue:1478,1483,1509,1528,1533,1535,1540,1581,1592,1594](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/WorkflowPage.vue)；[apps/user-web/src/utils/aiTokenGuard.js:48-49](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/utils/aiTokenGuard.js)
- **现象**：全局搜索 `window.$message =` 无任何赋值；`package.json` 依赖无 Element/Naive 等组件库。所有 `window.$message?.warning?.()` / `.success?.()` / `.error?.()` 均为静默空操作。
- **影响**：工作流页 AI 关键词提取、生图测试、润色等操作的成功/失败提示完全丢失，用户无任何反馈。
- **建议**：改为派发 `xya-toast` 全局事件（与 aiTokenGuard 一致），或在 App.vue 注入 provide/inject 的 message 服务。

### S9. user-web 注册页表单未填/未勾选协议时点击"立即注册"无任何提示

- **位置**：[apps/user-web/src/pages/RegisterPage.vue:12,125-127](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/RegisterPage.vue)
- **现象**：实测——未填邮箱/密码、未勾选协议时点击底部"立即注册" submit 按钮，`errorMsg` 已被赋值（"请先阅读并同意用户协议和隐私政策"），但视觉上无变化、无 toast。表单校验仅在 `submitRegister` 触发时才计算并显示 errorMsg，用户在点击前的字段级实时反馈缺失。
- **建议**：增加字段级失焦校验或表单顶层错误条；至少在点击 submit 时滚动到 errorMsg 并高亮错误字段。

### S10. admin-web 路由权限前缀匹配存在越权风险

- **位置**：[apps/admin-web/src/router/core/RoutePermissionValidator.ts:108-114](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/router/core/RoutePermissionValidator.ts)
- **现象**：`matchRoute` 中 `targetPath.startsWith(\`${routePath}/\`)` 让拥有 `/admin` 菜单权限的用户可访问 `/admin/任意未授权子路径`。等同于"父菜单可见则所有子路径可见"，与 RBAC 细粒度授权相悖。
- **建议**：删除通配前缀匹配，仅保留精确匹配 + 动态参数路由(`:id`)匹配。

### S11. admin-web 用户敏感信息明文持久化到 sessionStorage

- **位置**：[apps/admin-web/src/store/modules/user.ts:243-248,63,69](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/store/modules/user.ts)
- **现象**：`accessToken` 与 `lockPassword` 以**明文**存入 sessionStorage（key=`user`），XSS 攻击者可直接读取。`lockPassword` 是锁屏密码，本应仅做本地校验，却未做任何哈希。JSDoc 写"使用 localStorage 存储"与实际代码不符。
- **建议**：`lockPassword` 至少用 `SubtleCrypto` 做 SHA-256 + salt 哈希；`accessToken` 评估改用内存变量 + 短期 sessionStorage；同步修正 JSDoc。

### S12. admin-web 富文本编辑器：鉴权快照 + XSS 回显风险

- **位置**：[apps/admin-web/src/components/core/forms/art-wang-editor/index.vue:115-167](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/components/core/forms/art-wang-editor/index.vue)
- **现象**：
  - `editorConfig` 在 setup 阶段一次性构造，`headers: { Authorization: userStore.accessToken }` 是**快照值**，token 刷新后上传会带旧 token，导致 401；
  - 头部直接传 `userStore.accessToken`（无 `Bearer ` 前缀），与拦截器约定不一致；
  - 编辑器 `getHtml()` 输出与回显未经过 `sanitizeHtml`，存在存储型 XSS 风险。
- **建议**：`editorConfig` 改为 computed 或上传前动态读取；统一 Bearer 前缀；保存与回显前调用 `sanitizeHtml`。

### S13. crawler-service goofishItemDetail 超时返回 itemId 被当作成功

- **位置**：[goofishItemDetail.ts:178-183,261](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/goofishItemDetail.ts)；调用方 [server.ts:332-333](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/server.ts)
- **现象**：`Promise.race` 超时分支 `setTimeout(() => resolve({ itemId }), 10000)` 返回的对象没有 `ok/error` 字段区分成功失败。调用方 `res.status(200).json({ ok: true, detail })`，前端拿到 `ok: true` 但 `detail.picUrl/title/price` 全空。
- **建议**：超时分支返回 `{ itemId, ok: false, error: 'detail timeout' }`，调用方据此返回 422/504。

---

## 二、中等问题（潜在 Bug 与设计缺陷）

### M1. automation-service 异步上下文中调用同步阻塞 IO

- **位置**：[ws_token.py:107](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/app/services/ws_token.py)、[xianyu_goods_sync.py:108/544/1218](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/app/services/xianyu_goods_sync.py)
- **现象**：在 async 函数内直接 `requests.post`、`requests.get`、`time.sleep(random.uniform(...))`，会阻塞事件循环，影响所有协程（包括 WS 心跳）。
- **建议**：改用 `httpx.AsyncClient` 与 `asyncio.sleep`，或用 `asyncio.to_thread` 包裹。

### M2. automation-service 函数定义被覆盖导致死代码

- **位置**：[xianyu_api_service.py:258-259](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/app/services/xianyu_api_service.py)
- **现象**：`_get_account_auth = _get_account_auth_sync` 与 `_persist_account_auth_cookies = ...` 在模块加载时把异步版本覆盖，调用方实际走的是同步阻塞版本。
- **建议**：删除覆盖赋值，统一保留异步版本；同步入口用 `asyncio.run_coroutine_threadsafe` 显式封装。

### M3. automation-service 全局共享状态未加锁

- **位置**：`ws_client.py` 的 `_recent_text_sends`/`_AUTO_SOLVE_LAST_TS`、`ws_delivery_handler.py` 的 `_order_sync_last_run`/`_order_sync_tasks`、`notify_dispatcher.py` 的 `_ACCOUNT_STATUS_NOTIFIED`/`_NEW_ORDER_NOTIFIED`
- **现象**：跨协程读写 dict 未用 `asyncio.Lock` 保护，多账号并发时可能读到不一致状态。
- **建议**：用 `asyncio.Lock` 或改用 `contextvars`。

### M4. automation-service MySQL 引擎未配置 pool_recycle / pool_pre_ping

- **位置**：[app/core/database.py](file:///g:/源码/xianyu-assistant-package-temp/apps/automation-service/app/core/database.py)
- **现象**：`create_async_engine` 未配置 `pool_recycle`，MySQL 默认 `wait_timeout=28800`，长连接被服务端断开后下次请求会拿到失效连接。
- **建议**：`pool_recycle=3600`、`pool_pre_ping=True`。

### M5. automation-service 80+ 处 `except Exception: pass` 静默吞异常

- **位置**：`app/main.py`、`ws_storage.py`、`ws_protocol.py`、`cookie_token_refresher.py`、`internal.py`、`workflow.py`、`feishu_chat.py` 等共 80+ 处
- **影响**：关键错误被吞，线上排障几乎失明。
- **建议**：至少 `logger.exception("...")`；建立 CI 检查禁止裸 `except Exception: pass`。

### M6. crawler-service goofishSearch Promise.race 状态机不严谨

- **位置**：[goofishSearch.ts:416-472,480-537](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/goofishSearch.ts)
- **现象**：(a) MTOP 返回 `ret != SUCCESS`（Baxia 风控）时直接 `return`，既不 resolve 也不记录失败状态，主流程等满 6 秒；(b) 超时后走 DOM 兜底时 `page.on('response')` 监听器仍在运行，可能并发修改 `networkItems` 数组；(c) 多次 MTOP 响应时 `mtopResolve()` 后续是 no-op，但 `networkItems.push` 仍执行，等于丢弃。
- **建议**：引入 `mtopSettled` 标志位，第一次响应（无论成功失败）就 resolve 一个 `{ ok, items }` 对象；超时/DOM 兜底前先 `page.off('response', handler)`。

### M7. crawler-service worker.ts BullMQ 自动重试与 DB 状态转换存在重复执行窗口

- **位置**：[worker.ts:69-90](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/worker.ts)；[saveItems.ts:130-152](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/db/saveItems.ts)
- **现象**：`markCrawlJobRetrying` 把 DB 状态从 `running` 改回 `pending`，BullMQ 重试时 `claim` 又把 DB 改为 running，用户重新提交同店铺采集会命中这条 pending 任务，状态脱节。
- **建议**：retrying 时不要把 status 改回 `pending`，应新增 `retrying` 状态或记录 `next_attempt_at`。

### M8. user-web OpportunityPage 草稿自动保存定时器未在卸载时清理

- **位置**：[OpportunityPage.vue:465,2039](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/OpportunityPage.vue)
- **现象**：`draftAutoSaveTimer` 在 `triggerAutoSave()` 中创建，但组件未注册 `onBeforeUnmount` 清理。用户离开商机页后 2 秒后仍会执行 `saveDraft()`，读写已卸载组件的 reactive 状态。
- **建议**：补充 `onBeforeUnmount(() => { if (draftAutoSaveTimer) clearTimeout(draftAutoSaveTimer) })`。

### M9. user-web AccountsPage 擦亮轮询无最大超时保护

- **位置**：[AccountsPage.vue:2036](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/AccountsPage.vue)
- **现象**：`polishTimer = setInterval(() => { void poll() }, 1500)`，若后端持续返回 `running` 或网络抖动一直返回非终态，轮询永不停止（仅 catch 错误时停），无最大轮询次数/时长兜底。
- **建议**：增加 `MAX_POLISH_POLL_DURATION`（如 5 分钟），超时后停止并提示"擦亮超时，请稍后查看结果"。

### M10. user-web 后端 402（Token 余额不足）无前端友好处理

- **位置**：[request.js:177-192](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/utils/request.js)；[friendlyError.js](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/utils/friendlyError.js)
- **现象**：全局搜索 `402` 无任何业务处理。`aiTokenGuard` 注释称"由后端 precheck 402 兜底"，但 402 落到通用错误分支，用户只看到 httpErrorMessage 返回的通用文案，无充值引导。
- **建议**：在 request.js 拦截器对 402 派发 `xya-open-payment` + `xya-toast`，与 aiTokenGuard 保持一致。

### M11. user-web 长会话消息列表无虚拟滚动

- **位置**：[MessagesPage.vue:152](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/MessagesPage.vue) `v-for="m in contextMessages"`
- **现象**：单会话上千条消息时全量 v-for 渲染，DOM 节点随消息数线性增长。仓库未引入任何虚拟滚动库。
- **建议**：对消息列表引入轻量虚拟滚动（如 `vue-virtual-scroller`）或仅渲染最近 N 条 + 滚动加载。

### M12. user-web JWT token 存 localStorage

- **位置**：[auth.js:7,14](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/utils/auth.js)
- **现象**：一旦任意第三方脚本或 v-html 注入的脚本执行，可直接读 token。
- **建议**：改用 HttpOnly Cookie + CSRF token（后端已有媒体会话 Cookie 模式可参考），或至少配合 CSP 限制。

### M13. admin-web 动态路由 401 处理不完整留下脏状态

- **位置**：[router/guards/beforeEach.ts:389-396](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/router/guards/beforeEach.ts)
- **现象**：`doHandleDynamicRoutes` 的 401 分支只调用了 `userStore.setLoginStatus(false)` 和 `userStore.setToken('')`，**未调用 `userStore.logOut()`**。导致 HTTP 缓存、用户信息、iframe 路由缓存、router 重置、redirect 参数全部未清理。
- **建议**：统一调用 `userStore.logOut()`（它内部已包含 401 防抖逻辑）。

### M14. admin-web HTTP GET 请求去重无超时，可能永久阻塞

- **位置**：[utils/http/index.ts:65-68,318-324](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/utils/http/index.ts)
- **现象**：`pendingRequests` Map 中若某 GET 请求因网络挂死而永远不 resolve，后续相同 key 的请求都会 `return pending as Promise<T>` 永久 await，造成"接口卡死但无报错"假象。
- **建议**：为 pending Promise 增加 `Promise.race` 超时（如 15s）自动从 Map 移除并 reject。

### M15. core-api AutomationProxyController 单类 96 个端点

- **位置**：[AutomationProxyController.java](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/controller/AutomationProxyController.java) 1400+ 行、96 个 `@Mapping` 端点
- **影响**：职责混杂（SSE、上传、AI 改写、商品删除、健康检查等），维护成本高，code review 容易遗漏。
- **建议**：按职责拆分为 `SseProxyController`、`AiProxyController`、`UploadProxyController` 等。

### M16. core-api SSE 票据懒清理（内存泄漏）

- **位置**：[AutomationProxyController.java:69,696](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/controller/AutomationProxyController.java) `static final ConcurrentHashMap SSE_TICKETS`
- **现象**：仅在新建 ticket 时 `evictSseTickets()`。若长期没有新 ticket 请求，过期票据常驻内存；上限 10000 触发后拒绝新连接。
- **建议**：用 `ScheduledExecutorService` 定期清理，或改用 Caffeine 的 `expireAfterWrite`。

### M17. core-api SSE 流式调用无请求超时

- **位置**：[AutomationClient.java:167-171](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/AutomationClient.java) `streamSse()`
- **现象**：构建 HttpRequest 时未调用 `.timeout()`，下游 Python 若 hang 住，Java 端线程/输出流会无限期挂起。
- **建议**：设置一个较长但有限的超时（如 10 min），或显式心跳机制。

### M18. core-api 自实现 JWT，异常吞噬根因

- **位置**：[JwtUtil.java:99-153,171](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/security/JwtUtil.java)
- **现象**：自实现 JWT（不用 nimbus/jjwt），`verify()` 用 `catch (Exception e) { throw new IllegalArgumentException("token invalid"); }` 丢失所有根因；`sign()` 声明 `throws Exception`。
- **建议**：改用 Spring Security OAuth2 Resource Server 或 nimbus-jose-jwt；至少在 catch 内 `log.debug` 保留原始异常。

### M19. core-api 通知解析失败误判成功

- **位置**：[UserUtilityController.java:430,441,452,463](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/controller/UserUtilityController.java)
- **现象**：`parseFeishuResponse`/`parseDingtalkResponse`/`parseWechatWorkResponse`/`parsePushPlusResponse` 在 JSON 解析异常时 `catch (Exception e) { return true; }`，把无法解析的响应当成成功，会掩盖飞书/钉钉返回的错误。
- **建议**：解析异常返回 `false` 并 `log.warn` 记录原始 body。

### M20. core-api 控制器大量使用 `@RequestBody Map<String, Object>`

- **位置**：多处（如 `AdminContentController.java:34/39/58/71/76`、`AiBillingController.java:41/74/79`、`AutoDeliveryController.java:81/92/103`）
- **影响**：未使用 DTO + `@Valid`，参数校验全部下沉到 service，前端畸形请求无法在网关层早拦截；Swagger 文档失去类型信息。
- **建议**：定义 DTO + `@Valid` + `@NotNull/@NotBlank/@Size` 等约束。

### M21. user-web vite 构建配置 `dropConsole: true` + `sourcemap: false`

- **位置**：[vite.config.js:19-34](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/vite.config.js)
- **现象**：生产构建删除所有 `console.*` 且无 sourcemap。线上错误堆栈无法定位，排障成本高。
- **建议**：保留 `console.error`/`console.warn`（用 `dropConsole: false` + 显式过滤），或为生产构建生成 internal-only sourcemap 上传到错误监控服务。

---

## 三、轻微问题（代码异味与可维护性）

### L1. 超大文件清单

| 文件 | 行数 | 问题 |
|------|------|------|
| `automation-service/app/services/automation_runtime.py` | 约 8700 行 / 439KB | 单文件过大，IDE 与 AI 难以导航 |
| `automation-service/app/services/xianyu_goods_sync.py` | 2700+ 行 | 同上 |
| `automation-service/app/services/ws_storage.py` | 2300+ 行，`save_chat_message` 300+ 行 | 单函数圈复杂度极高 |
| `user-web/src/pages/MessagesPage.vue` | 3000+ 行 | 单组件过大 |
| `user-web/src/pages/AccountsPage.vue` | 2300+ 行 | 同上 |
| `user-web/src/pages/WorkflowPage.vue` | 2500+ 行 | 同上 |
| `user-web/src/pages/OpportunityPage.vue` | 2100+ 行 | 同上 |
| `crawler-service/src/crawler/sliderSolver.ts` | 2100+ 行 | 同上 |

建议：按职责拆分包/模块/子组件。

### L2. automation-service 重复导入与拼写错误

- `app/api/v1/routes/misc.py:31-43` 重复导入 `ws_manager`、`encrypt_cookie_for_storage`、`XianyuAccount`、`XianyuAccountAuth`
- `xianyu_api_service.py:514` `TOKEN_EXOIRED` 与 `TOKEN_EXPIRED` 并存

### L3. crawler-service TypeScript any 滥用

- `sliderSolver.ts:277,312,348,364,385,435-436,647-648,759,813` 共 11 处 `: any`
- `server.ts` / `worker.ts` / `goofish.ts` 中 16 处 `catch (e: any)`，应改用 `unknown` + 类型守卫

### L4. crawler-service dist 已入库但无 precommit/build 校验

- **位置**：[crawler-service/dist/crawler/goofishSearch.js](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/dist/crawler/goofishSearch.js)（已提交）；`package.json:14` `"build": "tsc"`
- **风险**：开发者改了 src 忘记 `npm run build` 就 `npm start` 会跑旧 dist。规则文件 `goofish-keyword-search.md` 明确要求"src 与 dist 必须同步"，但代码层面无校验。
- **建议**：dist 加入 `.gitignore`，CI 中 `tsc --noEmit` 校验；或加 husky precommit 钩子自动 build。

### L5. user-web 大量全局 window CustomEvent 通信，无类型安全

- `xya-toast` / `xya-open-payment` / `xya-auth-expired` / `xya-captcha-required` / `xya-sse-event` / `xya-header-action` 等事件名散落字符串。
- **建议**：封装 `bus.ts` 集中声明事件类型。

### L6. user-web 单文件超长 + deep:true 监听较大对象

- `OpportunityPage.vue:2107` 监听 7 个含对象 ref，deep:true
- `ProductPublishPage.vue:1353` selectedAddress deep
- 建议：监听具体子字段而非整对象 deep，或使用 watch 的 getter 形式精确指定。

### L7. admin-web v-auth / v-roles 指令无响应式更新

- **位置**：[directives/core/auth.ts:40-51](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/directives/core/auth.ts)、[roles.ts:52-72](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/directives/core/roles.ts)
- **现象**：指令 `mounted`/`updated` 钩子读取 `router.currentRoute.value.meta.authList` 与 `userStore.getUserInfo.roles`，但 `updated` 只在元素自身 binding 变化时触发，**不会响应 route meta 或 store 变化**。
- **建议**：改用 `<el-button v-if="hasAuth('add')">` 的 computed 方式。

### L8. admin-web sanitizeHtml URL 白名单与标签黑名单遗漏

- **位置**：[utils/sanitizeHtml.ts:1-6](file:///g:/源码/xianyu-assistant-package-temp/apps/admin-web/src/utils/sanitizeHtml.ts)
- **现象**：
  - `BLOCKED_TAGS` 未包含 `svg`、`math`、`foreignObject`、`animate`、`set` 等 SVG/命名切换标签
  - `SAFE_URL_PATTERN` 允许 `data:image/...`，但未排除 `data:image/svg+xml`（SVG 可含脚本）
  - `URL_ATTRS` 缺 `srcset`、`formaction`、`poster`、`background`、`dynsrc`
- **建议**：补全标签黑名单；`data:` 仅允许 `image/(png|jpeg|gif|webp)`；补全 URL 属性。

### L9. core-api 硬编码 10000 上限

- **位置**：[DashboardService.java:364](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/DashboardService.java) `accountMapper.list(tenantId, null, null, 0, 10000)`
- **影响**：超过 1 万账号的租户数据被截断。
- **建议**：改为流式查询或显式分页聚合。

### L10. core-api DashboardService schema 缓存永不失效

- **位置**：[WorkflowService.java:42](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/java/com/xianyu/admin/service/WorkflowService.java) `tableColumnExistsCache` 用 ConcurrentHashMap 缓存列存在性，应用生命周期内不刷新。
- **建议**：加 TTL 或在 schema 变更时显式失效。

### L11. crawler-service saveItems.ts 串行 INSERT 阻塞事件循环

- **位置**：[saveItems.ts:48-78](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/db/saveItems.ts)
- **现象**：1000 商品的店铺就是 1000 次 PG 往返，全部在单个事务里阻塞。Worker 是单并发（`concurrency: 1`）。
- **建议**：改用 `UNNEST($1::text[], $2::text[], ...)` 批量 INSERT，或分批（每 100 条一批）。

### L12. crawler-service goofishItemDetail DOM 兜底使用 networkidle 固定等待

- **位置**：[goofishItemDetail.ts:189](file:///g:/源码/xianyu-assistant-package-temp/apps/crawler-service/src/crawler/goofishItemDetail.ts)
- **现象**：`await page.waitForLoadState('networkidle', { timeout: 5000 })` 在长轮询页面（闲鱼商品页有 IM/埋点长连接）几乎永远等不到 networkidle，固定等满 5 秒。与文件顶部注释"事件驱动：用 Promise.race 竞速等待"的设计原则相悖。
- **建议**：改用 `page.waitForSelector('img[src*="alicdn"]', { timeout: 3000 })` 或直接走 og:image 提取。

---

## 四、浏览器测试发现的问题

### B1. 注册页存在两个"立即注册"按钮（视觉混淆）

- **复现**：访问 `http://localhost:5174/#/register`，snapshot 输出两个文本为"立即注册"的 button（`@e6` tab 头部、`@e11` 表单 submit）。
- **位置**：[RegisterPage.vue:12,125-127](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/RegisterPage.vue)
- **影响**：用户点击 tab 头部"立即注册"无反应，体验困惑。
- **建议**：tab 头部文案改为"账号注册"或"邮箱注册"以与底部 submit 按钮区分。

### B2. 注册页未填表单直接点 submit 时无视觉反馈

- **复现**：未填邮箱/密码、未勾选协议，点击底部"立即注册"，无 toast、无错误条显示，但 `errorMsg` 已被赋值。
- **位置**：[RegisterPage.vue:213-221,256-259](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/pages/RegisterPage.vue)
- **建议**：至少在表单顶部错误条显示 errorMsg（代码已有 `<div v-if="errorMsg" class="form-error">` 但实测未渲染，需检查 errorMsg 是否被 `validateForm()` 返回空字符串覆盖）。

### B3. 后端 JSON 中文乱码（详见 S1）

- **复现**：`Invoke-WebRequest http://localhost:18080/api/health` 返回 `{"msg":"æä½æå"}`，应为"操作成功"。
- **影响**：非浏览器客户端无法正常解析业务消息。

### B4. admin-web 登录页未完成滑块验证时点击登录无反馈

- **复现**：填账号 `admin`/密码 `wrongpass`，未拖动滑块，点击登录按钮，URL 跳到 `/auth/login?redirect=/` 但无 toast 提示，无网络请求发出。
- **位置**：滑块验证逻辑（`@e19` 滑块组件）
- **建议**：未完成滑块时点击登录应显示"请先完成滑块验证"提示。

### B5. 直接访问需要登录的路由会重定向到登录页（正常）

- **复现**：未登录访问 `http://localhost:5174/#/dashboard`，自动跳转回 `#/login`。
- **结论**：路由守卫工作正常。

### B6. 错误密码登录反馈正常（带 trace id）

- **复现**：浏览器端填 `nouser/wrongpass`，点击登录，form-error 显示"用户名或密码错误（错误编号：web-c5d357ff-cf77-4ca4-b8e9-550c627bec10）"。
- **结论**：错误反馈机制完整，trace id 可关联后端日志。

### B7. 用户协议/隐私政策按钮点击后无视觉反馈

- **复现**：登录页点击 `《用户协议》` 按钮，无 toast、无 dialog、无新窗口可见反馈（`openLegalDoc` 调用 `window.open`，headless 浏览器中弹窗被静默处理）。
- **位置**：[authContent.js:77-97](file:///g:/源码/xianyu-assistant-package-temp/apps/user-web/src/components/auth/authContent.js)
- **建议**：弹窗被阻止时应显示 form-error 提示"用户协议打开失败，请允许浏览器弹窗"。代码已有 `popup_blocked` 分支但未触发，需检查实际逻辑。

---

## 五、工程卫生问题

### H1. 项目根目录散落 54 个临时调试脚本（已被 git 追踪）

- **现象**：`git ls-files | Where-Object { $_ -match '^(_|\.tmp|test_|check_|fix_|debug_|deploy_|tmp_|admin-)' }` 返回 54 个文件，包括：
  - `_*.py` / `_*.js` / `_*.ps1`：调试脚本
  - `_*.png` / `_*.json`：截图与调试数据
  - `.tmp_*.py` / `.tmp_*.js`：临时修复脚本
  - `test_*.py` / `test_*.js` / `test_*.ps1`：手动测试脚本（非 pytest/jest）
  - `fix_*.py` / `debug_*.py` / `deploy_*.py`：一次性运维脚本
  - `admin-backend-analysis-report.md` / `admin-analysis-report.txt`：旧版分析报告（暴露了 5.1-5.7 安全问题清单）
- **影响**：仓库膨胀；旧报告可能误导；部分脚本含敏感信息（如 `_verify_token.txt` 是真实 JWT，虽已被 gitignore 但本地存在；`_create_redis_client.py` 可能含连接信息）。
- **建议**：批量 `git rm` 这些文件，扩充 `.gitignore`（添加 `/_*.py`、`/_*.js`、`/_*.ps1`、`/_*.json`、`/_*.png`、`/.tmp-*`、`/test_*.py`、`/test_*.js`、`/test_*.ps1`、`/check_*.py`、`/fix_*.py`、`/debug_*.py`、`/deploy_*.py`、`/admin-*-report.*`）。

### H2. _verify_token.txt 含真实 JWT（本地未提交但暴露）

- **位置**：[_verify_token.txt](file:///g:/源码/xianyu-assistant-package-temp/_verify_token.txt)
- **内容**：`eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxIiwidXNlck5hbWUiOiJkZW1vIiwidGVuYW50SWQiOiIxIiwiaWF0IjoxNzgzMjQ0MjQxLCJleHAiOjE3ODMzMzA2NDF9.KI1dy1B90Y231F0l0mC3UJXOu47CbY2uUHNu1fXwO7o`
- **状态**：已被 `.gitignore *_token.txt` 排除，但本地文件存在。
- **建议**：删除该文件；JWT 已过期（exp=1783330641=2026-07-04）。

### H3. admin-backend-analysis-report.md 已泄露历史安全问题清单

- **位置**：[admin-backend-analysis-report.md](file:///g:/源码/xianyu-assistant-package-temp/admin-backend-analysis-report.md)
- **现象**：26539 字节的旧分析报告，包含 5.1 JWT 密钥默认值、5.2 Cookie 加密密钥默认值、5.3 内部 API Token 默认值等安全问题清单。**已被 git 追踪**。
- **影响**：攻击者可从 git 历史直接获取安全问题清单作为攻击面参考。
- **建议**：`git rm` 该文件；扩充 `.gitignore`。

### H4. docker-compose.yml 默认弱口令

- **位置**：[docker-compose.yml:12,15,36,56,180](file:///g:/源码/xianyu-assistant-package-temp/docker-compose.yml)
- **现象**：
  - `MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}`
  - `MYSQL_PASSWORD: ${MYSQL_APP_PASSWORD:-dev-only-mysql-app-password-change-me}`
  - `REDIS_PASSWORD: ${REDIS_PASSWORD:-dev-only-redis-password-change-me}`
  - `POSTGRES_PASSWORD: ${CRAWLER_DB_PASSWORD:-crawler_pass}`
- **影响**：开发环境若忘记配 `.env`，会以弱口令启动数据库。
- **建议**：改为 `${MYSQL_ROOT_PASSWORD:?must be set}` 必填模式，与 `INTERNAL_API_TOKEN` 一致。

### H5. application.yml 默认弱口令 + dev profile 配置混乱

- **位置**：[application.yml:32,108,133,137,150](file:///g:/源码/xianyu-assistant-package-temp/apps/core-api/src/main/resources/application.yml)
- **现象**：
  - `password: 123456`（datasource 默认密码）
  - `admin.jwt.secret: ${ADMIN_JWT_SECRET:please-change-this-admin-jwt-secret-at-least-32-chars}`（弱默认值）
  - `cookie.crypto-secret: ${COOKIE_CRYPTO_SECRET:dev-only-cookie-crypto-secret-change-me-32-chars}`
  - `automation.internal-token: ${INTERNAL_API_TOKEN:dev-only-internal-api-token-change-me-32-chars}`
  - `sync.token: ${DATA_SYNC_API_TOKEN:HIDpsuvrKSlWfczLiFTJa0Ydhqm8gx7Q}`（**生产 token 直接写在 yml 中作为默认值**）
- **影响**：
  - 任意一项默认值若带入生产，对应安全机制失效
  - 数据同步 token 在源码中暴露，违反 `data-sync-bridge-token.md` 第 4 条"不得使用占位符值"——尽管此处不是占位符而是真实值，但**真实 token 不应硬编码在源码中**
- **建议**：删除所有 `dev-only-*-change-me` 与 `please-change-*` 默认值；`DATA_SYNC_API_TOKEN` 改为 `${DATA_SYNC_API_TOKEN:?must be set}`，本地联调通过 `.env` 注入。

---

## 六、合规性确认（已做对的部分）

- ✅ **AI Token 余额校验接入完整**：4 个通用模型调用场景（ProductPublishPage、OpportunityPage、AiCsSettings、WorkflowPage）均已调用 `ensureAiTokenBalance()`，符合 `general-model-per-call-billing.md` 第六节要求。
- ✅ **数据同步板块生产隐藏正确**：`.env.production` 设 `VITE_SHOW_DATA_SYNC=false`，`App.vue:158` 比较 `=== 'true'` 在生产构建时为 false，settings-sync 不进入 settingsKeys，符合 `data-sync-bridge-token.md` 第五节。
- ✅ **SSE 与媒体会话定时器清理完整**：App.vue `onBeforeUnmount` 清理 resize/hashchange/各种 CustomEvent 监听、mediaSessionTimer、noticeTimer、SSE，无明显泄漏。
- ✅ **multipart 文件上传限制正确**：`max-file-size: 20MB`、`max-request-size: 50MB`，符合项目约束。
- ✅ **Vite dev server 配置 /uploads 代理到 backend**：符合项目约束（不绕过 Java 鉴权直连 Python 文件目录）。
- ✅ **Java 网关代理 Python 服务时拆包 ResultObject**：AutomationProxyController 已统一拆包 `{code, msg, data}` 仅返回 data。
- ✅ **核心路由守卫工作正常**：未登录访问 `/#/dashboard` 会跳回 `/#/login`。
- ✅ **登录错误反馈带 trace id**：可关联后端日志。
- ✅ **数据库迁移基础设施完备**：`migrations-manifest.json` 含 sha256 校验，`SCHEMA_RUNTIME_MUTATIONS_ENABLED` 默认 `false`，符合 `database-migration-on-release.md`。
- ✅ **Docker 容器安全加固**：所有服务 `read_only: true`、`no-new-privileges:true`、`cap_drop: ALL`、tmpfs 限制 size。
- ✅ **CORS 配置使用具体 origin 而非 `*`**（虽然 `addAllowedHeader("*")` 仍偏宽，详见 core-api 报告 M11）。

---

## 七、优先修复建议路线图

### 第一阶段（本周内）：阻断安全风险

1. **S1** 后端 JSON 响应头加 `charset=utf-8`
2. **S2** AuthService 登录失败分支补 `consumePasswordVerification`
3. **S3** prod profile 强制开启 security-guard，删除所有 dev-only 默认值
4. **S5** DataSyncService SSRF 加 host 白名单
5. **S6** sliderSolver finally 块加 `fs.rm(userDataDir)`
6. **S10** admin-web RoutePermissionValidator 删除前缀匹配
7. **S12** admin-web 富文本编辑器接入 sanitizeHtml + 动态 token
8. **H3** `git rm admin-backend-analysis-report.md` 与 54 个临时脚本，扩充 `.gitignore`
9. **H5** 删除 `application.yml` 中所有弱默认值与硬编码 sync token

### 第二阶段（两周内）：修稳定性 Bug

1. **S4** DeliveryExecutionService 事务边界重构
2. **S7** crawler-service 注册全局未捕获异常处理器
3. **S8** user-web WorkflowPage `$message` 改为 `xya-toast` 事件
4. **S9** user-web RegisterPage 表单校验 UX 修复
5. **S11** admin-web lockPassword 哈希存储
6. **S13** crawler-service goofishItemDetail 超时返回区分成功失败
7. **M1-M7** automation-service 异步阻塞、状态机、80+ 静默 except 修整
8. **M8-M11** user-web 定时器清理、轮询超时、402 处理
9. **M13-M14** admin-web 401 脏状态、HTTP 去重超时

### 第三阶段（一月内）：可维护性优化

1. **L1** 拆分超大文件（automation_runtime.py、MessagesPage.vue 等）
2. **L4** crawler-service dist 出库 + precommit 校验
3. **L5** user-web CustomEvent 集中注册
4. **M15** AutomationProxyController 拆分为多个 Controller
5. **M20** core-api 控制器引入 DTO + `@Valid`
6. **M21** vite 构建保留 `console.error`/`console.warn`

---

## 附：扫描覆盖范围

| 模块 | 文件数 | 关键发现数 |
|------|--------|-----------|
| core-api (Java) | 200+ | 28 |
| automation-service (Python) | 50+ | 20 |
| crawler-service (Node/TS) | 16 | 15 |
| user-web (Vue3) | 100+ | 25 |
| admin-web (Vue3+TS) | 80+ | 15 |
| 根目录工程卫生 | - | 5 |
| 浏览器测试 | 7 场景 | 7 |
| **合计** | **450+ 文件** | **115 个发现** |

报告基于静态扫描 + 浏览器自动化测试，未运行时复现每个 bug。建议结合实际运行行为对严重问题进行修复并补充回归测试。
