请基于当前项目，完整重构并完善「自动发货」与「卡密仓库」功能，目标是实现一个对标闲管家、阿奇索、鱼店长等市面自动发货工具的虚拟商品发货系统。

## 一、产品目标

自动发货模块用于虚拟商品、数字商品、教程资料、网盘链接、激活码、兑换码、账号密码、充值卡、优惠券等商品的自动交付。

系统需要支持：

1. 付款后自动发货
2. 买家确认收货后自动赠送
3. 买家好评后自动赠送
4. 可选发货声明
5. 无卡密文字发货
6. 有卡密库存发货
7. 单商品配置
8. 多商品批量配置
9. 全部商品批量配置
10. 发货模板管理
11. 卡密仓库管理
12. 发货日志与失败重试
13. 库存预警与风控保护

最终目标是形成完整闭环：

商品配置 → 订单监听 → 发货条件判断 → 发货声明 → 内容组装 → 卡密分配 → 消息发送 → 状态回写 → 日志记录 → 异常处理。

## 二、页面结构参考

请参考市面自动发货工具的页面结构，将自动发货模块拆成以下几个页面或 Tab：

### 1. 发货设置

商品列表页面，展示当前账号下所有可配置自动发货的商品。

列表字段包括：

* 勾选框
* 商品图片
* 商品标题
* 商品 ID
* 售价
* 商品分类
* 所属闲鱼账号
* 付款后发货状态
* 收货后赠送状态
* 好评后赠送状态
* 发货模式
* 卡密库存
* 操作按钮

核心交互：

* 单商品设置付款后发货
* 单商品设置收货后赠送
* 单商品设置好评后赠送
* 批量设置付款后发货
* 批量设置收货后赠送
* 批量设置好评后赠送
* 批量删除所有发货配置
* 按账号筛选
* 按商品标题搜索
* 按商品 ID 搜索
* 按发货形式筛选
* 按内容方式筛选
* 按是否在售筛选
* 按是否已配置筛选

### 2. 发货声明

发货声明是可选功能。

当用户开启发货声明后，买家付款后系统先发送一段声明文案，买家确认后，再进入正式自动发货流程。

发货声明适用于虚拟商品、不可退换商品、需要买家确认风险的商品。

页面字段包括：

* 是否启用发货声明
* 声明文案编辑框
* 可插入变量
* 生效范围

支持变量：

* `{订单编号}`
* `{商品标题}`
* `{买家昵称}`
* `{发货确认链接}`

默认文案示例：

```text
订单编号：{订单编号}

您好，该订单包含的商品为虚拟商品，发货后不支持退换。如无异议，请点击下方链接确认发货。

{发货确认链接}
```

注意：

* 发货声明不是强制功能。
* 用户未配置时，不发送声明。
* 声明文案不能写死。
* 声明发送后，需要记录日志。
* 买家确认前，不进入正式发货。
* 如果声明确认链接过期，需要允许重新发送。

声明生效范围支持：

1. 全店所有自动发货商品生效
2. 仅对单独启用声明的商品生效

### 3. 模板管理

新增「发货模板」页面，用于管理可复用的发货内容模板。

模板字段：

* 模板名称
* 模板类型
* 模板状态
* 模板内容
* 是否加入随机模板列表
* 创建时间
* 最后修改时间

模板类型：

1. 付款后发货模板
2. 收货后赠送模板
3. 好评后赠送模板
4. 发货声明模板
5. 卡密发货模板
6. 普通文本模板

模板内容支持变量：

* `{买家昵称}`
* `{订单编号}`
* `{商品标题}`
* `{商品ID}`
* `{卡密}`
* `{卡号}`
* `{密码}`
* `{链接}`
* `{提取码}`
* `{当前时间}`
* `{店铺名称}`

需要支持分段发送变量：

```text
{分段}
```

当模板中出现 `{分段}` 时，系统需要把内容拆成多条消息依次发送。

示例：

```text
您好，感谢购买。

{分段}

您的卡密是：{卡密}

{分段}

请及时使用，如有问题请联系我。
```

最终发送为三条消息。

### 4. 卡密仓库

完善卡密仓库，支持多分组、多状态、多商品绑定、多店铺复用。

卡密仓库页面包括：

* 卡密分组列表
* 新建卡密分组
* 编辑卡密分组
* 删除卡密分组
* 导入卡密
* 导出卡密
* 卡密明细
* 使用记录
* 库存统计
* 库存预警

卡密分组字段：

* 分组名称
* 卡密类型
* 卡号前缀
* 密码前缀
* 成本单价
* 售价建议
* 库存预警阈值
* 备注
* 状态

卡密类型：

1. 唯一卡密
2. 卡号 + 密码
3. 链接 + 提取码
4. 账号 + 密码
5. 自定义文本

卡密状态：

* 未使用
* 已锁定
* 已使用
* 已作废
* 异常

库存统计：

* 总数量
* 未使用数量
* 已锁定数量
* 已使用数量
* 已作废数量
* 异常数量
* 今日发货数量
* 近 7 日发货数量

导入规则：

* 支持手动粘贴
* 支持 TXT 导入
* 支持 CSV 导入
* 一行一条
* 自动过滤空行
* 自动识别重复
* 导入后展示成功数量、重复数量、失败数量

导出规则：

* 支持按状态导出
* 支持按分组导出
* 支持导出使用记录
* 已使用卡密导出时必须包含订单号、买家、商品、使用时间

## 三、自动发货模式

系统需要支持两大类发货模式：

### 1. 文字发货，也叫无卡密发货

适用场景：

* 网盘链接
* 下载链接
* 教程资料
* 视频课程
* 使用说明
* 统一兑换地址
* 群二维码
* 固定服务说明

特点：

* 同一个商品，不同买家收到相同内容。
* 不消耗卡密库存。
* 支持分段发送。
* 支持头部和底部文案。

配置字段：

* 是否启用
* 发货时机
* 发货声明
* 消息头部
* 主体内容
* 消息底部
* 是否分段发送
* 失败重试次数
* 发送间隔

最终内容组装规则：

```text
{消息头部}

{主体内容}

{消息底部}
```

头部或底部为空时自动忽略，不生成多余空行。

### 2. 卡密发货，也叫有卡密发货

适用场景：

* 激活码
* CDK
* 充值卡
* 会员兑换码
* 软件序列号
* 游戏点卡
* 优惠券
* 账号密码

特点：

* 同一个商品，不同买家收到不同卡密。
* 卖一个少一个。
* 必须绑定卡密分组。
* 必须保证并发安全。
* 必须防止重复发货。

配置字段：

* 是否启用
* 发货时机
* 发货声明
* 绑定卡密分组
* 消息头部
* 卡密主体模板
* 消息底部
* 是否每张卡密单独发送
* 是否库存不足时自动停用
* 库存预警阈值

卡密主体模板示例：

```text
您的卡密是：{卡密}
```

如果卡密类型是卡号 + 密码，则支持：

```text
卡号：{卡号}
密码：{密码}
```

最终内容组装规则：

```text
{消息头部}

{卡密主体模板}

{消息底部}
```

## 四、发货时机

系统必须支持三种发货时机：

### 1. 付款后发货

触发条件：

* 订单状态为已付款 / 待发货
* 商品已开启付款后发货
* 当前订单未发过货
* 商品配置完整
* 如为卡密发货，库存充足

执行流程：

1. 检查订单状态
2. 检查商品配置
3. 检查是否重复发货
4. 如果配置了发货声明，先发送声明
5. 如果不需要声明或声明已确认，进入发货
6. 文字发货直接发送文本
7. 卡密发货先锁定卡密，再发送内容
8. 发送成功后更新订单发货状态
9. 记录发货日志

### 2. 确认收货后赠送

触发条件：

* 买家已确认收货
* 商品已开启收货后赠送
* 当前订单未执行过收货后赠送
* 配置完整

适用场景：

* 赠送使用说明
* 赠送体验券
* 赠送售后群链接
* 赠送额外资料
* 引导复购

需要支持：

* 全店统一设置
* 单商品独立设置
* 单商品优先级高于全店配置
* 某商品可设置为不赠送

### 3. 好评后赠送

触发条件：

* 买家已评价且为好评
* 商品已开启好评后赠送
* 当前订单未执行过好评后赠送
* 配置完整

适用场景：

* 好评返资料
* 好评赠送额外教程
* 好评赠送体验码
* 好评赠送优惠券

需要支持：

* 全店统一设置
* 单商品独立设置
* 单商品优先级高于全店配置
* 某商品可设置为不赠送

## 五、配置优先级

自动发货配置优先级如下：

1. 商品单独配置
2. 多商品批量配置
3. 全店统一配置
4. 默认不发货

如果商品单独设置了某种发货时机，则优先使用商品配置。

如果商品没有单独设置，则查看全店配置。

如果全店也没有配置，则不执行自动发货。

## 六、批量配置能力

需要支持以下批量操作：

1. 批量设置付款后发货
2. 批量设置收货后赠送
3. 批量设置好评后赠送
4. 批量关闭付款后发货
5. 批量关闭收货后赠送
6. 批量关闭好评后赠送
7. 批量绑定卡密分组
8. 批量应用发货模板
9. 批量删除所有发货配置
10. 一键配置全部商品

所有批量操作必须二次确认。

确认弹窗需要展示：

* 影响商品数量
* 影响发货时机
* 是否会覆盖已有配置
* 是否会启用自动发货
* 是否会绑定卡密分组

## 七、自动发货执行引擎

需要实现一个后台执行引擎，负责监听订单状态并触发发货。

执行引擎职责：

1. 拉取订单
2. 识别订单状态
3. 匹配商品
4. 查询商品发货配置
5. 判断发货时机
6. 判断是否已发过
7. 组装发货内容
8. 分配卡密
9. 调用消息发送能力
10. 更新卡密状态
11. 更新订单发货状态
12. 记录日志
13. 异常重试
14. 失败告警

需要保证幂等性：

* 同一个订单同一种发货时机只能执行一次
* 同一张卡密只能发给一个订单
* 重复订单推送不能重复扣减卡密
* 服务重启后不能重复发送
* 消息发送失败时不能把卡密标记为已使用

建议设计发货任务表，唯一键为：

```text
tenantId + accountId + orderId + goodsId + deliveryTiming
```

## 八、卡密并发安全

卡密分配必须原子化。

流程：

1. 开启事务
2. 查询一条未使用卡密
3. 原子更新为已锁定
4. 绑定订单号
5. 提交事务
6. 发送消息
7. 发送成功后标记为已使用
8. 发送失败时释放锁定或标记异常

禁止出现以下问题：

* 两个订单拿到同一张卡密
* 发送失败但卡密变成已使用
* 卡密已使用但没有发货日志
* 发货日志成功但卡密仍未使用
* 库存不足仍提示发货成功

## 九、发货日志

每次自动发货都必须记录日志。

日志字段：

* 租户 ID
* 闲鱼账号 ID
* 商品 ID
* 商品标题
* 订单 ID
* 买家 ID
* 买家昵称
* 发货时机
* 发货模式
* 使用卡密 ID
* 发送内容摘要
* 发送结果
* 失败原因
* 重试次数
* 创建时间
* 完成时间

日志状态：

* 待执行
* 执行中
* 成功
* 失败
* 已跳过
* 等待买家确认声明
* 库存不足
* 配置异常

前台需要提供：

* 发货日志列表
* 按商品筛选
* 按买家筛选
* 按订单筛选
* 按状态筛选
* 按时间筛选
* 失败重试
* 查看详情

## 十、异常处理

需要完整处理以下异常：

1. 商品未开启自动发货
2. 商品配置不完整
3. 卡密分组未绑定
4. 卡密库存不足
5. 卡密被并发锁定
6. 消息发送失败
7. 买家未确认发货声明
8. 订单状态不满足
9. 订单已发货
10. 重复订单推送
11. 买家已退款
12. 买家有历史订单未确认
13. 闲鱼账号离线
14. Cookie 失效
15. WebSocket 断开
16. 自动化服务异常
17. 租户余额不足
18. 商品已下架
19. 卡密分组被删除
20. 模板变量缺失

所有异常都不能静默失败，必须记录日志，并在前台显示明确原因。

## 十一、风控与安全策略

增加以下可选风控配置：

1. 前一笔订单未确认收货时限制继续发货
2. 同一买家短时间多次下单时暂停自动发货
3. 卡密库存不足时自动停用商品发货配置
4. 订单疑似退款风险时只发送声明不发送正式内容
5. 高风险买家进入人工审核
6. 发货内容过长时自动分段
7. 发送失败自动重试
8. 重试多次失败后转人工处理

## 十二、前台页面要求

### 自动发货首页

需要展示：

* 今日自动发货成功数
* 今日失败数
* 待处理订单数
* 库存不足商品数
* 已开启自动发货商品数
* 付款后发货配置数
* 收货后赠送配置数
* 好评后赠送配置数

### 发货设置页

需要对标截图中的布局：

* 左侧为发货菜单
* 中间为商品配置表格
* 表格中清晰展示三列：

  * 付款后发货
  * 收货后赠送
  * 好评后赠送
* 每一列展示当前状态：

  * 未设置
  * 已设置
  * 已停用
  * 库存不足
  * 配置异常
* 每一列都可以点击设置

### 发货声明页

需要提供：

* 声明开关
* 声明文案编辑
* 变量插入
* 生效范围
* 预览效果
* 保存按钮

### 模板管理页

需要提供：

* 模板列表
* 创建模板
* 编辑模板
* 删除模板
* 从模板库创建
* 加入随机模板
* 模板预览
* 变量说明

### 卡密仓库页

需要提供：

* 卡密分组
* 卡密导入
* 卡密库存
* 卡密明细
* 使用记录
* 库存预警
* 导出功能

## 十三、后端接口要求

至少需要实现以下接口：

### 发货配置接口

* 查询商品发货配置
* 保存单商品付款后发货配置
* 保存单商品收货后赠送配置
* 保存单商品好评后赠送配置
* 批量保存发货配置
* 一键应用到全部商品
* 关闭单商品发货配置
* 删除发货配置
* 查询全店默认配置

### 发货声明接口

* 查询发货声明
* 保存发货声明
* 启用 / 停用发货声明
* 预览发货声明

### 发货模板接口

* 查询模板列表
* 创建模板
* 更新模板
* 删除模板
* 复制模板
* 从模板库创建
* 查询可用变量

### 卡密仓库接口

* 创建卡密分组
* 更新卡密分组
* 删除卡密分组
* 导入卡密
* 查询卡密列表
* 查询库存统计
* 作废卡密
* 导出卡密
* 查询卡密使用记录

### 自动发货执行接口

* 扫描待发货订单
* 执行单个订单发货
* 重试失败发货
* 查询发货日志
* 查询发货详情

## 十四、数据表建议

建议新增或完善以下表：

1. `delivery_rule`

   * 存储商品级自动发货配置

2. `delivery_statement`

   * 存储发货声明配置

3. `delivery_template`

   * 存储发货模板

4. `card_group`

   * 存储卡密分组

5. `card_secret`

   * 存储卡密明细

6. `delivery_task`

   * 存储自动发货任务

7. `delivery_log`

   * 存储发货日志

8. `delivery_global_config`

   * 存储全店默认配置

所有表必须包含：

* `tenant_id`
* `account_id`
* `created_at`
* `updated_at`
* `deleted`

所有查询必须做租户隔离。

## 十五、验收标准

### 基础验收

1. 可以为单个商品设置付款后文字发货
2. 可以为单个商品设置付款后卡密发货
3. 可以为单个商品设置收货后赠送
4. 可以为单个商品设置好评后赠送
5. 可以配置可选发货声明
6. 可以配置消息头部、主体、底部
7. 可以使用模板快速创建发货内容
8. 可以导入卡密
9. 可以绑定卡密分组
10. 可以查看发货日志

### 批量验收

1. 可以批量配置多个商品
2. 可以一键配置全部商品
3. 批量操作有二次确认
4. 批量配置不会误覆盖用户不想覆盖的字段
5. 批量删除有风险提示

### 自动化验收

1. 订单付款后能自动触发付款后发货
2. 买家确认收货后能触发收货后赠送
3. 买家好评后能触发好评后赠送
4. 开启声明时先发送声明
5. 未配置声明时直接发货
6. 卡密库存不足时不发货并提示
7. 消息发送失败时不消耗卡密
8. 同一订单不会重复发货
9. 同一张卡密不会重复发送
10. 失败任务可以重试

### 安全验收

1. 所有数据按租户隔离
2. 不同闲鱼账号之间数据隔离
3. 卡密内容默认脱敏展示
4. 导出卡密需要权限确认
5. 删除卡密分组前检查商品绑定关系
6. 所有失败都有明确日志
7. 所有敏感内容不打印完整日志

## 十六、体验要求

页面风格需要与当前系统保持一致，但自动发货模块需要更接近专业工具：

* 表格清晰
* 状态明显
* 配置入口明确
* 批量操作方便
* 失败原因可见
* 库存风险可见
* 配置后能立即预览最终发送内容
* 发货日志可追踪
* 新用户可以按引导完成配置

请优先实现「付款后发货 + 文字发货 + 卡密发货 + 发货声明 + 卡密仓库 + 发货日志」闭环，再实现「收货后赠送」「好评后赠送」「分段发送」「历史订单未确认限制发货」等增强能力。
