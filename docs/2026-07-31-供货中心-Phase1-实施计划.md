# 供货中心 Phase 1 实施计划：货源管理与审核

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现供货商上传货源（文本/卡密）→ 后台审核 → 通过后上架到货源商城（与平台自营合并展示）的完整闭环。

**Architecture:** 新建 `supply_product` + `audit_record` 两张表，新建 3 个 Java 服务（SupplyProductService / SupplyAuditService / SupplyShopService）+ 4 个 Controller，改造现有 `DeliveryMallPage.vue` 实现 UNION 合并展示，新增 admin-web 审核工作台与权重调整页面。复用现有 `card_group`/`card_item` 卡密认领机制与 `notification` 通知体系。

**Tech Stack:** Java 17 + Spring Boot + JdbcTemplate + MyBatis（后端）；Vue 3 + Vite + Element Plus + Pinia（前端）；MySQL 8（数据库）；参考现有 `MallProductService` / `CardService` / `GrowthService` 代码风格。

**设计文档:** [2026-07-31-供货中心完整设计方案.md](./2026-07-31-供货中心完整设计方案.md)

---

## 文件结构

### 新建后端文件

| 文件 | 职责 |
|------|------|
| `apps/core-api/src/main/resources/db/migration/V1.66__create_supply_product_tables.sql` | 供货商品 + 审核记录建表 |
| `apps/core-api/src/main/resources/db/migration/V1.67__add_mall_product_weight.sql` | mall_product 加 weight 字段 |
| `apps/core-api/src/main/java/com/xianyu/admin/service/SupplyProductService.java` | 供货商品 CRUD + 上下架 + 改价 + 销量统计 |
| `apps/core-api/src/main/java/com/xianyu/admin/service/SupplyAuditService.java` | 审核提交/通过/驳回/历史查询 |
| `apps/core-api/src/main/java/com/xianyu/admin/service/SupplyShopService.java` | 货源商城 UNION 合并查询 |
| `apps/core-api/src/main/java/com/xianyu/admin/service/TradeConfigService.java` | 交易配置读写（sys_config） |
| `apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyProductController.java` | 供货商前台 API `/api/supply` |
| `apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyShopController.java` | 买家前台 API `/api/supply-shop` |
| `apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyAuditController.java` | 后台审核 API `/admin-api/supply/audit` |
| `apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyWeightController.java` | 后台权重 API `/admin-api/supply/weight` |

### 新建前端文件（user-web）

| 文件 | 职责 |
|------|------|
| `apps/user-web/src/pages/SupplyCenterPage.vue` | 供货中心主页 |
| `apps/user-web/src/pages/SupplyProductListPage.vue` | 我的货源列表 |
| `apps/user-web/src/pages/SupplyProductEditPage.vue` | 上传/编辑货源 |
| `apps/user-web/src/api/supply.js` | 供货商 API 封装 |

### 新建前端文件（admin-web）

| 文件 | 职责 |
|------|------|
| `apps/admin-web/src/views/admin/supply/audit/index.vue` | 审核工作台 |
| `apps/admin-web/src/views/admin/supply/weight/index.vue` | 权重调整 |
| `apps/admin-web/src/api/supply.ts` | 管理端 API 封装 |

### 改造现有文件

| 文件 | 改造内容 |
|------|---------|
| `apps/user-web/src/pages/DeliveryMallPage.vue` | 货源商城改为 UNION 查询，合并展示自营+供货 |
| `apps/user-web/src/api/mall.js` | 商城列表接口改调 `/api/supply-shop/products` |
| `apps/user-web/src/data/nav.js` | 新增"供货中心"导航入口 |
| `apps/user-web/src/router/index.js` | 新增供货中心路由 |
| `apps/admin-web/src/router/modules/admin.ts` | 新增审核/权重后台路由 |
| `apps/automation-service/app/services/notify_dispatcher.py` | 新增 2 个事件类型（审核通过/驳回） |

---

## 任务清单

### Task 1: 数据库迁移 - 供货商品与审核记录表

**Files:**
- Create: `apps/core-api/src/main/resources/db/migration/V1.66__create_supply_product_tables.sql`
- Create: `apps/core-api/src/main/resources/db/migration/V1.67__add_mall_product_weight.sql`

- [ ] **Step 1: 编写 V1.66 建表脚本**

参考设计文档第四章表 1 与表 8 的完整 SQL，创建 `supply_product` 和 `audit_record` 两张表。

注意遵循项目规则 `database-migration-on-release.md`：
- 使用 `CREATE TABLE IF NOT EXISTS`
- 仅追加，非破坏性 DDL
- 不使用 `ADD COLUMN IF NOT EXISTS`（MySQL 8.0 不支持），如需加列用 `INFORMATION_SCHEMA` 动态 SQL

`apps/core-api/src/main/resources/db/migration/V1.66__create_supply_product_tables.sql`:

```sql
-- 供货商品表
CREATE TABLE IF NOT EXISTS supply_product (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    seller_id BIGINT NOT NULL COMMENT '供货商用户ID',
    product_type VARCHAR(10) NOT NULL DEFAULT 'text' COMMENT 'text=文本货源, card=卡密货源',
    title VARCHAR(200) NOT NULL,
    subtitle VARCHAR(200) DEFAULT '',
    content TEXT COMMENT '商品描述/正文',
    delivery_content TEXT COMMENT '文本货源的发货内容',
    cover_url VARCHAR(500) DEFAULT '',
    images_json JSON COMMENT '商品图片数组',
    category VARCHAR(50) DEFAULT '' COMMENT 'AI分类',
    price_cent BIGINT NOT NULL DEFAULT 0 COMMENT '售价(分)',
    stock INT NOT NULL DEFAULT -1 COMMENT '库存(-1=无限,文本货源默认-1)',
    card_group_id BIGINT NULL COMMENT '卡密货源关联的 card_group.id',
    audit_status VARCHAR(20) NOT NULL DEFAULT 'pending' COMMENT 'pending/rejected/approved',
    audit_reason VARCHAR(500) DEFAULT '' COMMENT '驳回原因',
    audit_at DATETIME NULL,
    auditor_id BIGINT NULL,
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0=下架,1=上架(仅 audit_status=approved 时可上架)',
    weight INT NOT NULL DEFAULT 0 COMMENT '展示权重(越大越靠前,后台可调)',
    bought_count INT NOT NULL DEFAULT 0 COMMENT '销量',
    rating_avg DECIMAL(3,2) DEFAULT 5.00 COMMENT '平均评分',
    rating_count INT DEFAULT 0,
    sort_order INT DEFAULT 0,
    commission_rate DECIMAL(5,4) DEFAULT 0.0500 COMMENT '单品抽佣率(默认5%),0=用全局配置',
    created_time DATETIME,
    updated_time DATETIME,
    deleted TINYINT NOT NULL DEFAULT 0,
    INDEX idx_supply_seller(seller_id, deleted, audit_status),
    INDEX idx_supply_status(audit_status, status, deleted),
    INDEX idx_supply_category(category, status, deleted),
    INDEX idx_supply_card_group(card_group_id),
    INDEX idx_supply_weight(weight, status, deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='供货商品表';

-- 通用审核记录表
CREATE TABLE IF NOT EXISTS audit_record (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    tenant_id BIGINT NOT NULL,
    module_key VARCHAR(40) NOT NULL COMMENT 'supply_product/other_module',
    business_id BIGINT NOT NULL COMMENT '业务记录ID(如 supply_product.id)',
    submitter_id BIGINT NOT NULL,
    auditor_id BIGINT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'pending' COMMENT 'pending/approved/rejected',
    reason VARCHAR(500) DEFAULT '' COMMENT '驳回原因或通过备注',
    snapshot_json JSON COMMENT '提交时数据快照',
    submitted_at DATETIME,
    audited_at DATETIME,
    INDEX idx_audit_module(module_key, status),
    INDEX idx_audit_submitter(submitter_id, status),
    INDEX idx_audit_auditor(auditor_id, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通用审核记录';

-- card_group 加关联供货商品字段（兼容 MySQL 8.0 的动态 SQL）
SET @col_exists = (SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'card_group' AND COLUMN_NAME = 'linked_supply_product_id');
SET @sql = IF(@col_exists = 0,
    'ALTER TABLE card_group ADD COLUMN linked_supply_product_id BIGINT NULL COMMENT ''关联的供货商品ID''',
    'SELECT ''column linked_supply_product_id already exists'' AS msg');
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;
```

- [ ] **Step 2: 编写 V1.67 mall_product 加 weight 字段脚本**

`apps/core-api/src/main/resources/db/migration/V1.67__add_mall_product_weight.sql`:

```sql
-- mall_product 加 weight 字段（兼容 MySQL 8.0 的动态 SQL）
SET @col_exists = (SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'mall_product' AND COLUMN_NAME = 'weight');
SET @sql = IF(@col_exists = 0,
    'ALTER TABLE mall_product ADD COLUMN weight INT NOT NULL DEFAULT 0 COMMENT ''展示权重(越大越靠前)''',
    'SELECT ''column weight already exists'' AS msg');
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;

-- 为现有 mall_product 补充默认 weight=0（不影响现有排序）
UPDATE mall_product SET weight = 0 WHERE weight IS NULL;
```

- [ ] **Step 3: 本地执行迁移验证**

启动 core-api，观察日志确认 Flyway 自动执行 V1.66 和 V1.67 迁移成功。

```bash
# 启动 core-api（本地无需 Docker，MySQL 是系统服务）
cd apps/core-api
mvn spring-boot:run
```

验证：日志中出现 `Migrating schema "xianyu_assistant_admin" to version "1.66 - create supply product tables"` 和 `"1.67 - add mall product weight"`，无报错。

- [ ] **Step 4: 验证表结构**

```bash
& "C:\Program Files\mysql\bin\mysql.exe" -uroot -p123456 xianyu_assistant_admin -e "DESCRIBE supply_product; DESCRIBE audit_record; DESCRIBE card_group; DESCRIBE mall_product;"
```

验证：`supply_product` 和 `audit_record` 表存在且字段完整；`card_group` 有 `linked_supply_product_id` 字段；`mall_product` 有 `weight` 字段。

- [ ] **Step 5: 提交**

```bash
git add apps/core-api/src/main/resources/db/migration/V1.66__create_supply_product_tables.sql apps/core-api/src/main/resources/db/migration/V1.67__add_mall_product_weight.sql
git commit -m "feat(db): add supply_product and audit_record tables (V1.66, V1.67)"
```

---

### Task 2: TradeConfigService - 交易配置读写服务

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/service/TradeConfigService.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/service/NotificationConfigService.java`（sys_config 读写模式参考）

- [ ] **Step 1: 编写 TradeConfigService**

参考 `NotificationConfigService` 中 `sys_config` 表的读写模式。`sys_config` 表存储 key-value 配置。

`apps/core-api/src/main/java/com/xianyu/admin/service/TradeConfigService.java`:

```java
package com.xianyu.admin.service;

import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;

import java.math.BigDecimal;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

/**
 * 交易配置服务（基于 sys_config 表）
 */
@Service
public class TradeConfigService {

    private final JdbcTemplate jdbcTemplate;

    public TradeConfigService(JdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
    }

    /**
     * 读取配置值
     */
    public String get(String key, String defaultValue) {
        try {
            List<String> values = jdbcTemplate.queryForList(
                "SELECT config_value FROM sys_config WHERE config_key = ? AND deleted = 0 LIMIT 1",
                String.class, key);
            return values.isEmpty() ? defaultValue : values.get(0);
        } catch (Exception e) {
            return defaultValue;
        }
    }

    /**
     * 写入配置值（upsert）
     */
    public void set(String key, String value) {
        int updated = jdbcTemplate.update(
            "UPDATE sys_config SET config_value = ?, updated_time = NOW() WHERE config_key = ? AND deleted = 0",
            value, key);
        if (updated == 0) {
            jdbcTemplate.update(
                "INSERT INTO sys_config(config_key, config_value, created_time, updated_time, deleted) VALUES(?, ?, NOW(), NOW(), 0)",
                key, value);
        }
    }

    /**
     * 获取抽佣率（默认 0.0500）
     */
    public BigDecimal getCommissionRate() {
        return new BigDecimal(get("trade.commission_rate", "0.0500"));
    }

    /**
     * 获取冷冻天数（默认 7）
     */
    public int getFreezeDays() {
        return Integer.parseInt(get("trade.freeze_days", "7"));
    }

    /**
     * 获取自动完结小时数（默认 72）
     */
    public int getAutoCompleteHours() {
        return Integer.parseInt(get("trade.auto_complete_hours", "72"));
    }

    /**
     * 获取最低提现金额（分，默认 5000）
     */
    public long getMinWithdrawalAmountCent() {
        return Long.parseLong(get("trade.min_withdrawal_amount_cent", "5000"));
    }

    /**
     * 获取退款窗口天数（默认 7，等于冷冻期）
     */
    public int getRefundWindowDays() {
        return Integer.parseInt(get("trade.refund_window_days", "7"));
    }

    /**
     * 获取单个供货商最多上架商品数（默认 100）
     */
    public int getMaxProductsPerSeller() {
        return Integer.parseInt(get("trade.max_products_per_seller", "100"));
    }

    /**
     * 获取客服微信（默认 JiShu0724）
     */
    public String getCustomerServiceWechat() {
        return get("trade.customer_service_wechat", "JiShu0724");
    }

    /**
     * 获取全部交易配置
     */
    public Map<String, Object> getAll() {
        Map<String, Object> config = new HashMap<>();
        config.put("commission_rate", getCommissionRate());
        config.put("freeze_days", getFreezeDays());
        config.put("auto_complete_hours", getAutoCompleteHours());
        config.put("min_withdrawal_amount_cent", getMinWithdrawalAmountCent());
        config.put("refund_window_days", getRefundWindowDays());
        config.put("max_products_per_seller", getMaxProductsPerSeller());
        config.put("customer_service_wechat", getCustomerServiceWechat());
        return config;
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd apps/core-api
mvn compile -q
```

验证：编译无错误。

- [ ] **Step 3: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/service/TradeConfigService.java
git commit -m "feat(trade): add TradeConfigService for sys_config based trade settings"
```

---

### Task 3: SupplyProductService - 供货商品 CRUD 服务

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/service/SupplyProductService.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/service/MallProductService.java`（JdbcTemplate + 价格以分存储的模式参考）

- [ ] **Step 1: 阅读 MallProductService 学习现有模式**

阅读 `apps/core-api/src/main/java/com/xianyu/admin/service/MallProductService.java`，重点学习：
- JdbcTemplate 直写 SQL 的风格
- `price_cent` 以分为单位存储
- `tenant_id` 从 `UserContext` 获取
- 软删除 `deleted` 字段的使用
- 卡密商品库存查询（`SELECT COUNT(*) FROM mall_card_key WHERE product_id=? AND status='available'`）

- [ ] **Step 2: 编写 SupplyProductService**

`apps/core-api/src/main/java/com/xianyu/admin/service/SupplyProductService.java`:

```java
package com.xianyu.admin.service;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.xianyu.admin.security.UserContext;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.math.BigDecimal;
import java.util.*;

/**
 * 供货商品服务
 * 供货商上传/编辑/下架/补库存/改价；销量与库存看板
 */
@Service
public class SupplyProductService {

    private final JdbcTemplate jdbcTemplate;
    private final SupplyAuditService auditService;
    private final TradeConfigService configService;
    private final ObjectMapper objectMapper = new ObjectMapper();

    public SupplyProductService(JdbcTemplate jdbcTemplate,
                                 SupplyAuditService auditService,
                                 TradeConfigService configService) {
        this.jdbcTemplate = jdbcTemplate;
        this.auditService = auditService;
        this.configService = configService;
    }

    /**
     * 上传货源（触发审核）
     */
    @Transactional
    public Map<String, Object> createProduct(Map<String, Object> body) {
        Long userId = UserContext.currentUserId();
        Long tenantId = UserContext.currentTenantId();
        String productType = (String) body.getOrDefault("productType", "text");
        String title = (String) body.get("title");
        Long priceCent = ((Number) body.getOrDefault("priceCent", 0)).longValue();

        // 校验商品数量上限
        int maxProducts = configService.getMaxProductsPerSeller();
        Integer count = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product WHERE seller_id = ? AND deleted = 0",
            Integer.class, userId);
        if (count != null && count >= maxProducts) {
            throw new RuntimeException("已达供货商商品数量上限：" + maxProducts);
        }

        // 卡密货源校验 card_group_id 归属
        Long cardGroupId = body.get("cardGroupId") != null ? ((Number) body.get("cardGroupId")).longValue() : null;
        if ("card".equals(productType) && cardGroupId != null) {
            validateCardGroupOwnership(cardGroupId, userId);
        }

        // 写入商品
        jdbcTemplate.update(
            "INSERT INTO supply_product(tenant_id, seller_id, product_type, title, subtitle, content, " +
            "delivery_content, cover_url, images_json, category, price_cent, stock, card_group_id, " +
            "audit_status, status, weight, bought_count, commission_rate, created_time, updated_time, deleted) " +
            "VALUES(?,?,?,?,?,?,?,?,?,?,?, ?,?, 'pending', 0, 0, 0, ?, NOW(), NOW(), 0)",
            tenantId, userId, productType, title,
            body.getOrDefault("subtitle", ""),
            body.getOrDefault("content", ""),
            body.getOrDefault("deliveryContent", ""),
            body.getOrDefault("coverUrl", ""),
            body.get("imagesJson") != null ? body.get("imagesJson").toString() : null,
            body.getOrDefault("category", ""),
            priceCent,
            body.getOrDefault("stock", -1),
            cardGroupId,
            body.get("commissionRate") != null ? new BigDecimal(body.get("commissionRate").toString()) : new BigDecimal("0.0500")
        );

        Long productId = jdbcTemplate.queryForObject("SELECT LAST_INSERT_ID()", Long.class);

        // 提交审核
        auditService.submitForAudit("supply_product", productId, userId, tenantId, body);

        Map<String, Object> result = new HashMap<>();
        result.put("id", productId);
        result.put("auditStatus", "pending");
        result.put("message", "货源已提交，等待审核");
        return result;
    }

    /**
     * 我的货源列表
     */
    public Map<String, Object> listMyProducts(int page, int size, String auditStatus) {
        Long userId = UserContext.currentUserId();
        int offset = (page - 1) * size;

        StringBuilder where = new StringBuilder("WHERE seller_id = ? AND deleted = 0");
        List<Object> params = new ArrayList<>();
        params.add(userId);
        if (auditStatus != null && !auditStatus.isEmpty()) {
            where.append(" AND audit_status = ?");
            params.add(auditStatus);
        }

        Integer total = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product " + where, Integer.class, params.toArray());

        List<Map<String, Object>> products = jdbcTemplate.queryForList(
            "SELECT id, product_type, title, subtitle, cover_url, price_cent, stock, card_group_id, " +
            "audit_status, audit_reason, status, weight, bought_count, category, created_time, updated_time " +
            "FROM supply_product " + where +
            " ORDER BY created_time DESC LIMIT ? OFFSET ?",
            params.toArray());

        // 卡密商品补充实际库存
        for (Map<String, Object> product : products) {
            if ("card".equals(product.get("product_type")) && product.get("card_group_id") != null) {
                Long cardGroupId = ((Number) product.get("card_group_id")).longValue();
                Integer available = jdbcTemplate.queryForObject(
                    "SELECT COUNT(*) FROM card_item WHERE group_id = ? AND status = 0 AND deleted = 0",
                    Integer.class, cardGroupId);
                product.put("actualStock", available);
            }
        }

        Map<String, Object> result = new HashMap<>();
        result.put("total", total);
        result.put("list", products);
        result.put("page", page);
        result.put("size", size);
        return result;
    }

    /**
     * 货源详情
     */
    public Map<String, Object> getProduct(Long id) {
        List<Map<String, Object>> products = jdbcTemplate.queryForList(
            "SELECT * FROM supply_product WHERE id = ? AND deleted = 0", id);
        if (products.isEmpty()) {
            throw new RuntimeException("货源不存在");
        }
        Map<String, Object> product = products.get(0);
        // 卡密商品补充库存
        if ("card".equals(product.get("product_type")) && product.get("card_group_id") != null) {
            Long cardGroupId = ((Number) product.get("card_group_id")).longValue();
            Integer available = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM card_item WHERE group_id = ? AND status = 0 AND deleted = 0",
                Integer.class, cardGroupId);
            product.put("actualStock", available);
        }
        return product;
    }

    /**
     * 编辑货源
     * 改标题/描述/封面 → 重新审核；改价 → 直接生效
     */
    @Transactional
    public Map<String, Object> updateProduct(Long id, Map<String, Object> body) {
        Long userId = UserContext.currentUserId();
        validateOwnership(id, userId);

        boolean needReaudit = false;
        List<String> updates = new ArrayList<>();
        List<Object> params = new ArrayList<>();

        if (body.containsKey("title")) {
            updates.add("title = ?");
            params.add(body.get("title"));
            needReaudit = true;
        }
        if (body.containsKey("subtitle")) {
            updates.add("subtitle = ?");
            params.add(body.get("subtitle"));
            needReaudit = true;
        }
        if (body.containsKey("content")) {
            updates.add("content = ?");
            params.add(body.get("content"));
            needReaudit = true;
        }
        if (body.containsKey("coverUrl")) {
            updates.add("cover_url = ?");
            params.add(body.get("coverUrl"));
            needReaudit = true;
        }
        if (body.containsKey("deliveryContent")) {
            updates.add("delivery_content = ?");
            params.add(body.get("deliveryContent"));
            needReaudit = true;
        }
        // 改价直接生效，无需重新审核
        if (body.containsKey("priceCent")) {
            updates.add("price_cent = ?");
            params.add(((Number) body.get("priceCent")).longValue());
        }
        if (body.containsKey("weight")) {
            updates.add("weight = ?");
            params.add(body.get("weight"));
        }

        if (updates.isEmpty()) {
            Map<String, Object> result = new HashMap<>();
            result.put("message", "无更新内容");
            return result;
        }

        updates.add("updated_time = NOW()");
        params.add(id);

        jdbcTemplate.update(
            "UPDATE supply_product SET " + String.join(", ", updates) + " WHERE id = ?",
            params.toArray());

        // 如果改了标题/描述/封面，需要重新审核
        if (needReaudit) {
            jdbcTemplate.update(
                "UPDATE supply_product SET audit_status = 'pending' WHERE id = ?", id);
            // 如果当前是上架状态，先下架
            jdbcTemplate.update(
                "UPDATE supply_product SET status = 0 WHERE id = ? AND status = 1", id);
            auditService.submitForAudit("supply_product", id, userId,
                UserContext.currentTenantId(), body);
        }

        Map<String, Object> result = new HashMap<>();
        result.put("message", needReaudit ? "已修改，需重新审核" : "修改成功");
        result.put("needReaudit", needReaudit);
        return result;
    }

    /**
     * 上架（仅 audit_status=approved）
     */
    public Map<String, Object> online(Long id) {
        Long userId = UserContext.currentUserId();
        validateOwnership(id, userId);

        Integer auditStatusCount = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product WHERE id = ? AND audit_status = 'approved' AND deleted = 0",
            Integer.class, id);
        if (auditStatusCount == null || auditStatusCount == 0) {
            throw new RuntimeException("审核未通过，无法上架");
        }

        jdbcTemplate.update("UPDATE supply_product SET status = 1, updated_time = NOW() WHERE id = ?", id);

        Map<String, Object> result = new HashMap<>();
        result.put("message", "上架成功");
        return result;
    }

    /**
     * 下架
     */
    public Map<String, Object> offline(Long id) {
        Long userId = UserContext.currentUserId();
        validateOwnership(id, userId);
        jdbcTemplate.update("UPDATE supply_product SET status = 0, updated_time = NOW() WHERE id = ?", id);
        Map<String, Object> result = new HashMap<>();
        result.put("message", "下架成功");
        return result;
    }

    /**
     * 删除（软删，有销量则禁止）
     */
    public Map<String, Object> delete(Long id) {
        Long userId = UserContext.currentUserId();
        validateOwnership(id, userId);

        Integer boughtCount = jdbcTemplate.queryForObject(
            "SELECT bought_count FROM supply_product WHERE id = ?", Integer.class, id);
        if (boughtCount != null && boughtCount > 0) {
            throw new RuntimeException("已有销量的商品不可删除");
        }

        jdbcTemplate.update("UPDATE supply_product SET deleted = 1, status = 0, updated_time = NOW() WHERE id = ?", id);
        Map<String, Object> result = new HashMap<>();
        result.put("message", "删除成功");
        return result;
    }

    /**
     * 销量/库存/收入统计
     */
    public Map<String, Object> stats(Long id) {
        Long userId = UserContext.currentUserId();
        validateOwnership(id, userId);

        Map<String, Object> stats = jdbcTemplate.queryForList(
            "SELECT bought_count, price_cent, stock, card_group_id, product_type, status, audit_status " +
            "FROM supply_product WHERE id = ?", id).get(0);

        // 卡密商品补充实际库存
        if ("card".equals(stats.get("product_type")) && stats.get("card_group_id") != null) {
            Long cardGroupId = ((Number) stats.get("card_group_id")).longValue();
            Integer available = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM card_item WHERE group_id = ? AND status = 0 AND deleted = 0",
                Integer.class, cardGroupId);
            stats.put("actualStock", available);
        }

        return stats;
    }

    /**
     * 供货中心首页数据（已上传数/今日收入/收入趋势/余额占位）
     * 注：今日收入与余额在 Phase 2 trade_balance 实现后填充真实数据
     */
    public Map<String, Object> dashboard() {
        Long userId = UserContext.currentUserId();

        Integer uploadedCount = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product WHERE seller_id = ? AND deleted = 0",
            Integer.class, userId);

        Integer pendingAuditCount = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product WHERE seller_id = ? AND audit_status = 'pending' AND deleted = 0",
            Integer.class, userId);

        Integer onlineCount = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product WHERE seller_id = ? AND status = 1 AND deleted = 0",
            Integer.class, userId);

        Map<String, Object> dashboard = new HashMap<>();
        dashboard.put("uploadedCount", uploadedCount);
        dashboard.put("pendingAuditCount", pendingAuditCount);
        dashboard.put("onlineCount", onlineCount);
        // Phase 2 后填充真实收入数据
        dashboard.put("todayIncomeCent", 0);
        dashboard.put("availableBalanceCent", 0);
        dashboard.put("frozenBalanceCent", 0);
        return dashboard;
    }

    /**
     * 收入流水趋势（近30天）
     * Phase 2 实现（依赖 cash_balance_ledger 表）
     */
    public Map<String, Object> salesTrend() {
        Map<String, Object> result = new HashMap<>();
        result.put("message", "收入趋势将在 Phase 2 交易功能上线后可用");
        result.put("trend", Collections.emptyList());
        return result;
    }

    /**
     * 校验商品归属
     */
    private void validateOwnership(Long productId, Long userId) {
        Integer count = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM supply_product WHERE id = ? AND seller_id = ? AND deleted = 0",
            Integer.class, productId, userId);
        if (count == null || count == 0) {
            throw new RuntimeException("无权操作此商品");
        }
    }

    /**
     * 校验卡密组归属
     */
    private void validateCardGroupOwnership(Long cardGroupId, Long userId) {
        Integer count = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM card_group WHERE id = ? AND user_id = ? AND deleted = 0",
            Integer.class, cardGroupId, userId);
        if (count == null || count == 0) {
            throw new RuntimeException("卡密组不存在或无权使用");
        }
    }
}
```

- [ ] **Step 3: 编译验证**

```bash
cd apps/core-api
mvn compile -q
```

验证：编译无错误。

- [ ] **Step 4: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/service/SupplyProductService.java
git commit -m "feat(supply): add SupplyProductService for supplier product CRUD"
```

---

### Task 4: SupplyAuditService - 审核服务

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/service/SupplyAuditService.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/service/AiCsLearnedKbService.java`（review_status 模式参考）

- [ ] **Step 1: 编写 SupplyAuditService**

`apps/core-api/src/main/java/com/xianyu/admin/service/SupplyAuditService.java`:

```java
package com.xianyu.admin.service;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.xianyu.admin.security.AdminContext;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.*;

/**
 * 供货商品审核服务
 * 提交审核 / 通过 / 驳回 / 历史查询
 */
@Service
public class SupplyAuditService {

    private final JdbcTemplate jdbcTemplate;
    private final TradeNotifyService notifyService;
    private final ObjectMapper objectMapper = new ObjectMapper();

    public SupplyAuditService(JdbcTemplate jdbcTemplate, TradeNotifyService notifyService) {
        this.jdbcTemplate = jdbcTemplate;
        this.notifyService = notifyService;
    }

    /**
     * 提交审核
     */
    @Transactional
    public void submitForAudit(String moduleKey, Long businessId, Long submitterId,
                                Long tenantId, Map<String, Object> snapshot) {
        try {
            String snapshotJson = objectMapper.writeValueAsString(snapshot);
            jdbcTemplate.update(
                "INSERT INTO audit_record(tenant_id, module_key, business_id, submitter_id, " +
                "status, snapshot_json, submitted_at) VALUES(?, ?, ?, ?, 'pending', ?, NOW())",
                tenantId, moduleKey, businessId, submitterId, snapshotJson);
        } catch (Exception e) {
            throw new RuntimeException("提交审核失败: " + e.getMessage());
        }
    }

    /**
     * 待审核列表（后台）
     */
    public Map<String, Object> pendingList(int page, int size, String moduleKey) {
        int offset = (page - 1) * size;
        StringBuilder where = new StringBuilder("WHERE a.status = 'pending'");
        List<Object> params = new ArrayList<>();
        if (moduleKey != null && !moduleKey.isEmpty()) {
            where.append(" AND a.module_key = ?");
            params.add(moduleKey);
        }

        Integer total = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM audit_record a " + where, Integer.class, params.toArray());

        List<Map<String, Object>> list = jdbcTemplate.queryForList(
            "SELECT a.*, u.username as submitter_name " +
            "FROM audit_record a LEFT JOIN sys_user u ON a.submitter_id = u.id " +
            where + " ORDER BY a.submitted_at ASC LIMIT ? OFFSET ?",
            params.toArray());

        // 供货商品审核补充商品标题
        for (Map<String, Object> item : list) {
            if ("supply_product".equals(item.get("module_key"))) {
                Long businessId = ((Number) item.get("business_id")).longValue();
                List<Map<String, Object>> products = jdbcTemplate.queryForList(
                    "SELECT title, cover_url, price_cent, product_type, seller_id FROM supply_product WHERE id = ?",
                    businessId);
                if (!products.isEmpty()) {
                    item.put("productInfo", products.get(0));
                }
            }
        }

        Map<String, Object> result = new HashMap<>();
        result.put("total", total);
        result.put("list", list);
        result.put("page", page);
        result.put("size", size);
        return result;
    }

    /**
     * 审核通过
     */
    @Transactional
    public Map<String, Object> approve(Long auditId, String reason) {
        Long auditorId = AdminContext.currentAdminId();

        Map<String, Object> audit = getAuditRecord(auditId);
        String moduleKey = (String) audit.get("module_key");
        Long businessId = ((Number) audit.get("business_id")).longValue();
        Long submitterId = ((Number) audit.get("submitter_id")).longValue();

        // 更新审核记录
        jdbcTemplate.update(
            "UPDATE audit_record SET status = 'approved', auditor_id = ?, reason = ?, audited_at = NOW() " +
            "WHERE id = ? AND status = 'pending'",
            auditorId, reason != null ? reason : "", auditId);

        // 更新业务表状态
        if ("supply_product".equals(moduleKey)) {
            jdbcTemplate.update(
                "UPDATE supply_product SET audit_status = 'approved', audit_at = NOW(), auditor_id = ? WHERE id = ?",
                auditorId, businessId);
        }

        // 通知供货商
        notifyService.notifyAuditApproved(submitterId, businessId, moduleKey);

        Map<String, Object> result = new HashMap<>();
        result.put("message", "审核通过");
        return result;
    }

    /**
     * 审核驳回
     */
    @Transactional
    public Map<String, Object> reject(Long auditId, String reason) {
        Long auditorId = AdminContext.currentAdminId();

        Map<String, Object> audit = getAuditRecord(auditId);
        String moduleKey = (String) audit.get("module_key");
        Long businessId = ((Number) audit.get("business_id")).longValue();
        Long submitterId = ((Number) audit.get("submitter_id")).longValue();

        // 更新审核记录
        jdbcTemplate.update(
            "UPDATE audit_record SET status = 'rejected', auditor_id = ?, reason = ?, audited_at = NOW() " +
            "WHERE id = ? AND status = 'pending'",
            auditorId, reason, auditId);

        // 更新业务表状态
        if ("supply_product".equals(moduleKey)) {
            jdbcTemplate.update(
                "UPDATE supply_product SET audit_status = 'rejected', audit_reason = ?, audit_at = NOW(), auditor_id = ? WHERE id = ?",
                reason, auditorId, businessId);
        }

        // 通知供货商
        notifyService.notifyAuditRejected(submitterId, businessId, moduleKey, reason);

        Map<String, Object> result = new HashMap<>();
        result.put("message", "已驳回");
        return result;
    }

    /**
     * 审核历史
     */
    public Map<String, Object> history(int page, int size, String moduleKey, String status) {
        int offset = (page - 1) * size;
        StringBuilder where = new StringBuilder("WHERE 1=1");
        List<Object> params = new ArrayList<>();
        if (moduleKey != null && !moduleKey.isEmpty()) {
            where.append(" AND module_key = ?");
            params.add(moduleKey);
        }
        if (status != null && !status.isEmpty()) {
            where.append(" AND status = ?");
            params.add(status);
        }

        Integer total = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM audit_record " + where, Integer.class, params.toArray());

        List<Map<String, Object>> list = jdbcTemplate.queryForList(
            "SELECT a.*, u.username as submitter_name, au.username as auditor_name " +
            "FROM audit_record a " +
            "LEFT JOIN sys_user u ON a.submitter_id = u.id " +
            "LEFT JOIN sys_admin_user au ON a.auditor_id = au.id " +
            where + " ORDER BY a.submitted_at DESC LIMIT ? OFFSET ?",
            params.toArray());

        Map<String, Object> result = new HashMap<>();
        result.put("total", total);
        result.put("list", list);
        result.put("page", page);
        result.put("size", size);
        return result;
    }

    private Map<String, Object> getAuditRecord(Long auditId) {
        List<Map<String, Object>> records = jdbcTemplate.queryForList(
            "SELECT * FROM audit_record WHERE id = ?", auditId);
        if (records.isEmpty()) {
            throw new RuntimeException("审核记录不存在");
        }
        return records.get(0);
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd apps/core-api
mvn compile -q
```

验证：编译无错误（注意：依赖 TradeNotifyService，下一个 Task 创建）。

如果编译报 `TradeNotifyService` 找不到，先创建一个空壳版本，下个 Task 再填充通知逻辑。

- [ ] **Step 3: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/service/SupplyAuditService.java
git commit -m "feat(supply): add SupplyAuditService for product review workflow"
```

---

### Task 5: TradeNotifyService - 通知服务

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/service/TradeNotifyService.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/service/UserNotificationService.java`（站内信写入模式参考）

- [ ] **Step 1: 编写 TradeNotifyService**

参考 `UserNotificationService.insertInAppNotification()` 的站内信写入模式。

`apps/core-api/src/main/java/com/xianyu/admin/service/TradeNotifyService.java`:

```java
package com.xianyu.admin.service;

import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;

import java.util.HashMap;
import java.util.Map;

/**
 * 供货中心通知服务
 * 封装交易场景通知，调用 notification 表写入站内信
 */
@Service
public class TradeNotifyService {

    private final JdbcTemplate jdbcTemplate;

    public TradeNotifyService(JdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
    }

    /**
     * 通知供货商：审核通过
     */
    public void notifyAuditApproved(Long userId, Long businessId, String moduleKey) {
        Map<String, Object> extra = new HashMap<>();
        extra.put("productId", businessId);
        extra.put("moduleKey", moduleKey);
        insertNotification(userId, "货源审核通过",
            "您的货源（ID: " + businessId + "）已通过审核，可前往供货中心上架。", "supply_audit_approved", extra);
    }

    /**
     * 通知供货商：审核驳回
     */
    public void notifyAuditRejected(Long userId, Long businessId, String moduleKey, String reason) {
        Map<String, Object> extra = new HashMap<>();
        extra.put("productId", businessId);
        extra.put("moduleKey", moduleKey);
        extra.put("reason", reason);
        insertNotification(userId, "货源审核驳回",
            "您的货源（ID: " + businessId + "）审核未通过，原因：" + reason + "。请修改后重新提交。", "supply_audit_rejected", extra);
    }

    /**
     * 写入站内信到 notification 表
     * 参考 UserNotificationService.insertInAppNotification
     */
    private void insertNotification(Long userId, String title, String content, String noticeType, Map<String, Object> extra) {
        try {
            jdbcTemplate.update(
                "INSERT INTO notification(tenant_id, user_id, notice_type, notification_type, title, content, " +
                "level, priority, is_read, created_time) " +
                "SELECT tenant_id, ?, ?, ?, ?, ?, 'normal', 0, 0, NOW() FROM sys_user WHERE id = ?",
                userId, noticeType, noticeType, title, content, userId);
        } catch (Exception e) {
            // 通知发送失败不影响主流程
        }
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd apps/core-api
mvn compile -q
```

验证：编译无错误。

- [ ] **Step 3: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/service/TradeNotifyService.java
git commit -m "feat(trade): add TradeNotifyService for in-app notifications"
```

---

### Task 6: SupplyShopService - 货源商城合并查询服务

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/service/SupplyShopService.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/service/MallProductService.java`（商城列表查询模式参考）

- [ ] **Step 1: 编写 SupplyShopService**

实现 UNION 查询 `mall_product` + `supply_product`，按 weight 排序。

`apps/core-api/src/main/java/com/xianyu/admin/service/SupplyShopService.java`:

```java
package com.xianyu.admin.service;

import com.xianyu.admin.security.UserContext;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;

import java.util.*;

/**
 * 货源商城合并查询服务
 * UNION mall_product（平台自营）+ supply_product（供货商品）
 */
@Service
public class SupplyShopService {

    private final JdbcTemplate jdbcTemplate;

    public SupplyShopService(JdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
    }

    /**
     * 商城列表（合并展示）
     */
    public Map<String, Object> listProducts(int page, int size, String category, String keyword) {
        int offset = (page - 1) * size;

        // UNION 查询：mall_product + supply_product
        // 统一字段：id, source, title, subtitle, cover_url, price_cent, product_type, category, bought_count, weight
        StringBuilder unionSql = new StringBuilder(
            "SELECT id, 'mall' as source, title, subtitle, cover_url, price_cent, " +
            "'text' as product_type, category, bought_count, weight, sort_order, created_time " +
            "FROM mall_product WHERE status = 1 AND deleted = 0 " +
            "UNION ALL " +
            "SELECT id, 'supply' as source, title, subtitle, cover_url, price_cent, " +
            "product_type, category, bought_count, weight, sort_order, created_time " +
            "FROM supply_product WHERE status = 1 AND audit_status = 'approved' AND deleted = 0"
        );

        List<Object> params = new ArrayList<>();

        // 分类筛选（在 UNION 外部用 WHERE 过滤）
        StringBuilder whereClause = new StringBuilder();
        if (category != null && !category.isEmpty()) {
            whereClause.append(" WHERE category = ?");
            params.add(category);
        }
        if (keyword != null && !keyword.isEmpty()) {
            whereClause.append(whereClause.length() > 0 ? " AND" : " WHERE");
            whereClause.append(" title LIKE ?");
            params.add("%" + keyword + "%");
        }

        // 统计总数
        String countSql = "SELECT COUNT(*) FROM (" + unionSql + ") AS merged" + whereClause;
        Integer total = jdbcTemplate.queryForObject(countSql, Integer.class, params.toArray());

        // 查询列表（按 weight DESC, sort_order DESC, created_time DESC 排序）
        String listSql = "SELECT * FROM (" + unionSql + ") AS merged" + whereClause +
            " ORDER BY weight DESC, sort_order DESC, created_time DESC LIMIT ? OFFSET ?";
        List<Object> listParams = new ArrayList<>(params);
        listParams.add(size);
        listParams.add(offset);

        List<Map<String, Object>> list = jdbcTemplate.queryForList(listSql, listParams.toArray());

        // 供货商品补充卡密实际库存
        for (Map<String, Object> product : list) {
            if ("supply".equals(product.get("source")) && "card".equals(product.get("product_type"))) {
                Long productId = ((Number) product.get("id")).longValue();
                Integer stock = getCardStock(productId);
                product.put("stock", stock);
            }
        }

        Map<String, Object> result = new HashMap<>();
        result.put("total", total);
        result.put("list", list);
        result.put("page", page);
        result.put("size", size);
        return result;
    }

    /**
     * 商品详情（按 source 分发）
     */
    public Map<String, Object> getProductDetail(String source, Long id) {
        if ("mall".equals(source)) {
            return getMallProductDetail(id);
        } else {
            return getSupplyProductDetail(id);
        }
    }

    private Map<String, Object> getMallProductDetail(Long id) {
        List<Map<String, Object>> products = jdbcTemplate.queryForList(
            "SELECT id, 'mall' as source, title, subtitle, content, cover_url, price_cent, " +
            "category, bought_count, status FROM mall_product WHERE id = ? AND status = 1 AND deleted = 0", id);
        if (products.isEmpty()) {
            throw new RuntimeException("商品不存在");
        }
        return products.get(0);
    }

    private Map<String, Object> getSupplyProductDetail(Long id) {
        List<Map<String, Object>> products = jdbcTemplate.queryForList(
            "SELECT id, 'supply' as source, title, subtitle, content, cover_url, price_cent, " +
            "product_type, category, bought_count, status, card_group_id, seller_id " +
            "FROM supply_product WHERE id = ? AND status = 1 AND audit_status = 'approved' AND deleted = 0", id);
        if (products.isEmpty()) {
            throw new RuntimeException("商品不存在");
        }
        Map<String, Object> product = products.get(0);
        if ("card".equals(product.get("product_type"))) {
            product.put("stock", getCardStock(id));
        }
        return product;
    }

    /**
     * 获取供货商品的卡密库存
     */
    private Integer getCardStock(Long productId) {
        List<Long> cardGroupIds = jdbcTemplate.queryForList(
            "SELECT card_group_id FROM supply_product WHERE id = ? AND card_group_id IS NOT NULL",
            Long.class, productId);
        if (cardGroupIds.isEmpty()) {
            return 0;
        }
        Long cardGroupId = cardGroupIds.get(0);
        return jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM card_item WHERE group_id = ? AND status = 0 AND deleted = 0",
            Integer.class, cardGroupId);
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd apps/core-api
mvn compile -q
```

验证：编译无错误。

- [ ] **Step 3: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/service/SupplyShopService.java
git commit -m "feat(supply): add SupplyShopService for unified mall+supply product query"
```

---

### Task 7: 供货商前台 Controller

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyProductController.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/controller/CardController.java`（UserContext 鉴权模式参考）

- [ ] **Step 1: 编写 SupplyProductController**

参考 `CardController` 的 `@RequestMapping("/api/cards")` + `UserContext.currentUserId()` 模式。

`apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyProductController.java`:

```java
package com.xianyu.admin.controller;

import com.xianyu.admin.service.SupplyProductService;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

/**
 * 供货商前台 API
 */
@RestController
@RequestMapping("/api/supply")
public class SupplyProductController {

    private final SupplyProductService productService;

    public SupplyProductController(SupplyProductService productService) {
        this.productService = productService;
    }

    @PostMapping("/products")
    public Map<String, Object> createProduct(@RequestBody Map<String, Object> body) {
        return productService.createProduct(body);
    }

    @GetMapping("/products")
    public Map<String, Object> listProducts(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String auditStatus) {
        return productService.listMyProducts(page, size, auditStatus);
    }

    @GetMapping("/products/{id}")
    public Map<String, Object> getProduct(@PathVariable Long id) {
        return productService.getProduct(id);
    }

    @PutMapping("/products/{id}")
    public Map<String, Object> updateProduct(@PathVariable Long id, @RequestBody Map<String, Object> body) {
        return productService.updateProduct(id, body);
    }

    @PostMapping("/products/{id}/online")
    public Map<String, Object> online(@PathVariable Long id) {
        return productService.online(id);
    }

    @PostMapping("/products/{id}/offline")
    public Map<String, Object> offline(@PathVariable Long id) {
        return productService.offline(id);
    }

    @DeleteMapping("/products/{id}")
    public Map<String, Object> delete(@PathVariable Long id) {
        return productService.delete(id);
    }

    @GetMapping("/products/{id}/stats")
    public Map<String, Object> stats(@PathVariable Long id) {
        return productService.stats(id);
    }

    @GetMapping("/dashboard")
    public Map<String, Object> dashboard() {
        return productService.dashboard();
    }

    @GetMapping("/sales/trend")
    public Map<String, Object> salesTrend() {
        return productService.salesTrend();
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd apps/core-api
mvn compile -q
```

- [ ] **Step 3: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyProductController.java
git commit -m "feat(supply): add SupplyProductController for supplier frontend API"
```

---

### Task 8: 买家前台商城 Controller

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyShopController.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/controller/MallProductShopController.java`（前台购买模式参考）

- [ ] **Step 1: 编写 SupplyShopController**

`apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyShopController.java`:

```java
package com.xianyu.admin.controller;

import com.xianyu.admin.service.SupplyShopService;
import com.xianyu.admin.service.TradeConfigService;
import org.springframework.web.bind.annotation.*;

import java.util.HashMap;
import java.util.Map;

/**
 * 买家前台货源商城 API
 */
@RestController
@RequestMapping("/api/supply-shop")
public class SupplyShopController {

    private final SupplyShopService shopService;
    private final TradeConfigService configService;

    public SupplyShopController(SupplyShopService shopService, TradeConfigService configService) {
        this.shopService = shopService;
        this.configService = configService;
    }

    @GetMapping("/products")
    public Map<String, Object> listProducts(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String category,
            @RequestParam(required = false) String keyword) {
        return shopService.listProducts(page, size, category, keyword);
    }

    @GetMapping("/products/{source}/{id}")
    public Map<String, Object> getProductDetail(@PathVariable String source, @PathVariable Long id) {
        return shopService.getProductDetail(source, id);
    }

    /**
     * 购买接口（Phase 2 实现完整支付流程，此处先返回提示）
     */
    @PostMapping("/purchase")
    public Map<String, Object> purchase(@RequestBody Map<String, Object> body) {
        Map<String, Object> result = new HashMap<>();
        result.put("message", "购买功能将在 Phase 2 交易功能上线后可用");
        result.put("code", "NOT_IMPLEMENTED");
        return result;
    }

    /**
     * 获取客服微信
     */
    @GetMapping("/customer-service")
    public Map<String, Object> customerService() {
        Map<String, Object> result = new HashMap<>();
        result.put("wechat", configService.getCustomerServiceWechat());
        return result;
    }
}
```

- [ ] **Step 2: 编译验证 + 启动测试**

```bash
cd apps/core-api
mvn compile -q
mvn spring-boot:run
```

验证：启动无报错，访问 `http://localhost:18080/api/supply-shop/products?page=1&size=10` 返回空列表（暂无供货商品）。

- [ ] **Step 3: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/controller/SupplyShopController.java
git commit -m "feat(supply): add SupplyShopController for buyer mall API"
```

---

### Task 9: 后台审核 + 权重 Controller

**Files:**
- Create: `apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyAuditController.java`
- Create: `apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyWeightController.java`
- Reference: `apps/core-api/src/main/java/com/xianyu/admin/controller/AdminGrowthController.java`（AdminContext 鉴权模式参考）

- [ ] **Step 1: 编写 AdminSupplyAuditController**

`apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyAuditController.java`:

```java
package com.xianyu.admin.controller;

import com.xianyu.admin.service.SupplyAuditService;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

/**
 * 后台审核工作台 API
 */
@RestController
@RequestMapping("/admin-api/supply/audit")
public class AdminSupplyAuditController {

    private final SupplyAuditService auditService;

    public AdminSupplyAuditController(SupplyAuditService auditService) {
        this.auditService = auditService;
    }

    @GetMapping("/pending")
    public Map<String, Object> pendingList(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String moduleKey) {
        return auditService.pendingList(page, size, moduleKey);
    }

    @PostMapping("/{id}/approve")
    public Map<String, Object> approve(@PathVariable Long id, @RequestBody(required = false) Map<String, Object> body) {
        String reason = body != null ? (String) body.get("reason") : "";
        return auditService.approve(id, reason);
    }

    @PostMapping("/{id}/reject")
    public Map<String, Object> reject(@PathVariable Long id, @RequestBody Map<String, Object> body) {
        String reason = (String) body.get("reason");
        return auditService.reject(id, reason);
    }

    @GetMapping("/history")
    public Map<String, Object> history(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String moduleKey,
            @RequestParam(required = false) String status) {
        return auditService.history(page, size, moduleKey, status);
    }
}
```

- [ ] **Step 2: 编写 AdminSupplyWeightController**

`apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyWeightController.java`:

```java
package com.xianyu.admin.controller;

import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.web.bind.annotation.*;

import java.util.HashMap;
import java.util.Map;

/**
 * 后台商品权重调整 API
 */
@RestController
@RequestMapping("/admin-api/supply/weight")
public class AdminSupplyWeightController {

    private final JdbcTemplate jdbcTemplate;

    public AdminSupplyWeightController(JdbcTemplate jdbcTemplate) {
        this.jdbcTemplate = jdbcTemplate;
    }

    @PutMapping
    public Map<String, Object> updateWeight(@RequestBody Map<String, Object> body) {
        String source = (String) body.get("source");
        Long id = ((Number) body.get("id")).longValue();
        Integer weight = ((Number) body.get("weight")).intValue();

        String table = "mall".equals(source) ? "mall_product" : "supply_product";
        int updated = jdbcTemplate.update(
            "UPDATE " + table + " SET weight = ?, updated_time = NOW() WHERE id = ?",
            weight, id);

        Map<String, Object> result = new HashMap<>();
        result.put("message", updated > 0 ? "权重已更新" : "商品不存在");
        result.put("updated", updated);
        return result;
    }
}
```

- [ ] **Step 3: 编译验证 + 启动测试**

```bash
cd apps/core-api
mvn compile -q
mvn spring-boot:run
```

验证：启动无报错。后台登录后访问 `http://localhost:18080/admin-api/supply/audit/pending?page=1&size=10` 返回空列表。

- [ ] **Step 4: 提交**

```bash
git add apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyAuditController.java apps/core-api/src/main/java/com/xianyu/admin/controller/AdminSupplyWeightController.java
git commit -m "feat(supply): add admin controllers for audit workbench and weight adjustment"
```

---

### Task 10: 后端集成验证

**Files:**
- 无新建，验证已有代码

- [ ] **Step 1: 启动后端服务**

```bash
cd apps/core-api
mvn spring-boot:run
```

- [ ] **Step 2: 验证供货商上传货源 API**

```bash
# 登录获取 token（前台 admin/123456）
$token = (Invoke-RestMethod -Uri "http://localhost:18080/api/auth/login" -Method Post -Body @{username="admin";password="123456"}).token

# 上传一个文本货源
Invoke-RestMethod -Uri "http://localhost:18080/api/supply/products" -Method Post -Headers @{Authorization="Bearer $token"} -Body @{
    productType="text"
    title="测试文本货源"
    content="这是一个测试货源"
    deliveryContent="发货内容示例"
    priceCent=500
} -ContentType "application/json"
```

验证：返回 `{id: 1, auditStatus: "pending", message: "货源已提交，等待审核"}`。

- [ ] **Step 3: 验证后台审核 API**

```bash
# 后台登录
$adminToken = (Invoke-RestMethod -Uri "http://localhost:18080/admin-api/auth/login" -Method Post -Body @{username="admin";password="123456"}).token

# 查看待审核列表
Invoke-RestMethod -Uri "http://localhost:18080/admin-api/supply/audit/pending" -Headers @{Authorization="Bearer $adminToken"}

# 审核通过
Invoke-RestMethod -Uri "http://localhost:18080/admin-api/supply/audit/1/approve" -Method Post -Headers @{Authorization="Bearer $adminToken"} -Body @{reason=""} -ContentType "application/json"
```

验证：待审核列表返回刚才上传的商品；审核通过后 `supply_product.audit_status` 变为 `approved`。

- [ ] **Step 4: 验证上架与商城展示**

```bash
# 供货商上架
Invoke-RestMethod -Uri "http://localhost:18080/api/supply/products/1/online" -Method Post -Headers @{Authorization="Bearer $token"}

# 商城列表查询（UNION）
Invoke-RestMethod -Uri "http://localhost:18080/api/supply-shop/products?page=1&size=10"
```

验证：商城列表包含刚上架的供货商品，`source=supply`。

- [ ] **Step 5: 提交（如有修复）**

```bash
git add -A
git commit -m "test(supply): verify backend integration for Phase 1"
```

---

### Task 11: 前端 API 封装

**Files:**
- Create: `apps/user-web/src/api/supply.js`
- Create: `apps/admin-web/src/api/supply.ts`
- Reference: `apps/user-web/src/api/mall.js` / `apps/admin-web/src/api/mall.ts`

- [ ] **Step 1: 编写 user-web supply.js**

`apps/user-web/src/api/supply.js`:

```javascript
import request from '../utils/request'

// 供货商品管理
export function createSupplyProduct(data) {
  return request({ url: '/supply/products', method: 'post', data })
}

export function getSupplyProducts(params) {
  return request({ url: '/supply/products', method: 'get', params })
}

export function getSupplyProductDetail(id) {
  return request({ url: `/supply/products/${id}`, method: 'get' })
}

export function updateSupplyProduct(id, data) {
  return request({ url: `/supply/products/${id}`, method: 'put', data })
}

export function onlineSupplyProduct(id) {
  return request({ url: `/supply/products/${id}/online`, method: 'post' })
}

export function offlineSupplyProduct(id) {
  return request({ url: `/supply/products/${id}/offline`, method: 'post' })
}

export function deleteSupplyProduct(id) {
  return request({ url: `/supply/products/${id}`, method: 'delete' })
}

export function getSupplyProductStats(id) {
  return request({ url: `/supply/products/${id}/stats`, method: 'get' })
}

// 供货中心首页
export function getSupplyDashboard() {
  return request({ url: '/supply/dashboard', method: 'get' })
}

export function getSupplySalesTrend() {
  return request({ url: '/supply/sales/trend', method: 'get' })
}

// 货源商城（买家）
export function getSupplyShopProducts(params) {
  return request({ url: '/supply-shop/products', method: 'get', params })
}

export function getSupplyShopProductDetail(source, id) {
  return request({ url: `/supply-shop/products/${source}/${id}`, method: 'get' })
}

export function getCustomerServiceWechat() {
  return request({ url: '/supply-shop/customer-service', method: 'get' })
}
```

- [ ] **Step 2: 编写 admin-web supply.ts**

`apps/admin-web/src/api/supply.ts`:

```typescript
import request from '@/utils/request'

// 审核工作台
export function getPendingAuditList(params: { page: number; size: number; moduleKey?: string }) {
  return request({ url: '/admin-api/supply/audit/pending', method: 'get', params })
}

export function approveAudit(id: number, reason?: string) {
  return request({ url: `/admin-api/supply/audit/${id}/approve`, method: 'post', data: { reason: reason || '' } })
}

export function rejectAudit(id: number, reason: string) {
  return request({ url: `/admin-api/supply/audit/${id}/reject`, method: 'post', data: { reason } })
}

export function getAuditHistory(params: { page: number; size: number; moduleKey?: string; status?: string }) {
  return request({ url: '/admin-api/supply/audit/history', method: 'get', params })
}

// 权重调整
export function updateProductWeight(data: { source: string; id: number; weight: number }) {
  return request({ url: '/admin-api/supply/weight', method: 'put', data })
}
```

- [ ] **Step 3: 提交**

```bash
git add apps/user-web/src/api/supply.js apps/admin-web/src/api/supply.ts
git commit -m "feat(supply): add frontend API wrappers for supply center"
```

---

### Task 12: 供货中心前端页面

**Files:**
- Create: `apps/user-web/src/pages/SupplyCenterPage.vue`
- Create: `apps/user-web/src/pages/SupplyProductListPage.vue`
- Create: `apps/user-web/src/pages/SupplyProductEditPage.vue`
- Modify: `apps/user-web/src/router/index.js`（新增路由）
- Modify: `apps/user-web/src/data/nav.js`（新增导航入口）
- Reference: `apps/user-web/src/pages/ProfileCenterPage.vue`（设计系统：主色 #0d6bff、圆角 16px、彩色卡片）

- [ ] **Step 1: 编写 SupplyCenterPage.vue**

参考设计文档第八章 8.1 节的布局，遵循现有设计系统（主色 #0d6bff，圆角 16px，渐变横幅，彩色统计卡片）。

页面包含：
- 顶部渐变横幅（欢迎语）
- 左侧个人信息板
- 右侧 4 张彩色统计卡片（已上传数/今日收入/可用余额/冻结余额）
- 收入流水趋势图占位（Phase 2 实现）
- 快捷操作按钮
- 待办提醒

调用 API：
- `getSupplyDashboard()` 获取统计数据
- `useUserStore()` 获取用户信息

- [ ] **Step 2: 编写 SupplyProductListPage.vue**

货源列表页面，包含：
- 审核状态筛选 tab（全部/待审核/已通过/已驳回）
- 商品表格（标题、类型、价格、库存、审核状态、上架状态、操作）
- 操作按钮：编辑/上架/下架/删除/查看统计

调用 API：
- `getSupplyProducts(params)` 列表
- `onlineSupplyProduct(id)` / `offlineSupplyProduct(id)` / `deleteSupplyProduct(id)`

- [ ] **Step 3: 编写 SupplyProductEditPage.vue**

上传/编辑货源页面，包含：
- 文本货源 / 卡密货源 tab 切换
- 通用字段：标题、副标题、描述、封面图、价格、分类
- 文本货源：发货内容输入框
- 卡密货源：卡密组选择器（关联 `card_group`）
- 提交按钮（触发审核）

调用 API：
- `createSupplyProduct(data)` 新建
- `updateSupplyProduct(id, data)` 编辑
- 复用 `getCardGroups()` 获取卡密组列表

- [ ] **Step 4: 添加路由与导航**

在 `apps/user-web/src/router/index.js` 新增路由：
```javascript
{ path: '/supply-center', component: () => import('../pages/SupplyCenterPage.vue') },
{ path: '/supply-center/products', component: () => import('../pages/SupplyProductListPage.vue') },
{ path: '/supply-center/products/new', component: () => import('../pages/SupplyProductEditPage.vue') },
{ path: '/supply-center/products/:id/edit', component: () => import('../pages/SupplyProductEditPage.vue') },
```

在 `apps/user-web/src/data/nav.js` 新增导航入口（参考现有 `deliveryMall` 等入口格式）：
```javascript
{ key: 'supply-center', label: '供货中心', icon: 'shop' }
```

- [ ] **Step 5: 启动前端验证**

```bash
cd apps/user-web
npm run dev
```

验证：
- 访问 `http://localhost:5174/supply-center` 显示供货中心主页
- 统计卡片正确显示（已上传数等）
- 点击"上传货源"跳转到编辑页
- 上传成功后跳转回列表页，显示"待审核"状态

- [ ] **Step 6: 提交**

```bash
git add apps/user-web/src/pages/SupplyCenterPage.vue apps/user-web/src/pages/SupplyProductListPage.vue apps/user-web/src/pages/SupplyProductEditPage.vue apps/user-web/src/router/index.js apps/user-web/src/data/nav.js
git commit -m "feat(supply): add supplier center frontend pages (dashboard, list, edit)"
```

---

### Task 13: 后台审核工作台页面

**Files:**
- Create: `apps/admin-web/src/views/admin/supply/audit/index.vue`
- Create: `apps/admin-web/src/views/admin/supply/weight/index.vue`
- Modify: `apps/admin-web/src/router/modules/admin.ts`（新增路由）
- Reference: `apps/admin-web/src/views/admin/feedback/index.vue`（审核工作台模式参考）

- [ ] **Step 1: 编写审核工作台页面**

`apps/admin-web/src/views/admin/supply/audit/index.vue`:

包含：
- 待审核 / 已审 tab 切换
- 待审核列表：商品标题、类型、价格、提交人、提交时间、操作（通过/驳回）
- 驳回弹窗：填写驳回原因
- 已审历史列表：审核结果、审核人、审核时间、原因

调用 API：
- `getPendingAuditList(params)` / `getAuditHistory(params)`
- `approveAudit(id, reason)` / `rejectAudit(id, reason)`

- [ ] **Step 2: 编写权重调整页面**

`apps/admin-web/src/views/admin/supply/weight/index.vue`:

包含：
- 商品来源筛选（全部/平台自营/供货商品）
- 商品列表：标题、来源、当前权重、权重输入框、保存按钮
- 权重说明提示

调用 API：
- 复用 `getSupplyShopProducts` 获取商品列表（含 weight 字段）
- `updateProductWeight({source, id, weight})` 保存

- [ ] **Step 3: 添加后台路由**

在 `apps/admin-web/src/router/modules/admin.ts` 新增：
```typescript
{ path: 'supply/audit', component: () => import('@/views/admin/supply/audit/index.vue') },
{ path: 'supply/weight', component: () => import('@/views/admin/supply/weight/index.vue') },
```

- [ ] **Step 4: 启动后台验证**

```bash
cd apps/admin-web
npm run dev
```

验证：
- 访问 `http://localhost:3006/admin/supply/audit` 显示审核工作台
- 待审核列表正确显示
- 点击"通过"审核成功，商品状态更新
- 点击"驳回"弹出原因输入框，提交后通知发送

- [ ] **Step 5: 提交**

```bash
git add apps/admin-web/src/views/admin/supply/ apps/admin-web/src/router/modules/admin.ts
git commit -m "feat(supply): add admin workbench for audit and weight management"
```

---

### Task 14: 改造货源商城页面（UNION 合并展示）

**Files:**
- Modify: `apps/user-web/src/pages/DeliveryMallPage.vue`
- Modify: `apps/user-web/src/api/mall.js`
- Reference: 现有 `DeliveryMallPage.vue` 的卡片渲染逻辑

- [ ] **Step 1: 修改 mall.js API 调用**

将商品列表接口从 `/mall/products` 改为 `/supply-shop/products`（UNION 查询）。

在 `apps/user-web/src/api/mall.js` 中新增或修改：

```javascript
// 货源商城列表（合并展示自营+供货）
export function getMallProducts(params) {
  return request({ url: '/supply-shop/products', method: 'get', params })
}

// 商品详情（按 source 分发）
export function getMallProductDetail(source, id) {
  return request({ url: `/supply-shop/products/${source}/${id}`, method: 'get' })
}
```

注意：保留现有 `/mall/products` 接口的导出函数名，避免大量改动调用处。如果现有 `getMallProductDetail` 只接收 `id` 参数，需要兼容处理（默认 source='mall'）。

- [ ] **Step 2: 修改 DeliveryMallPage.vue 卡片渲染**

改造 `DeliveryMallPage.vue`：
- 列表数据现在包含 `source` 字段（`mall`/`supply`）
- 卡片渲染不区分来源，统一样式
- 点击卡片跳转详情时，根据 `source` 调用 `getMallProductDetail(source, id)`
- 购买按钮暂时禁用或提示"Phase 2 上线"（Phase 2 实现完整购买流程）

关键改动点：
```vue
<!-- 卡片点击 -->
<template v-for="product in products" :key="`${product.source}-${product.id}`">
  <div class="product-card" @click="goDetail(product)">
    <!-- 统一卡片样式，不区分 source -->
  </div>
</template>

<script>
function goDetail(product) {
  router.push(`/delivery-mall/${product.source}/${product.id}`)
}
</script>
```

- [ ] **Step 3: 启动验证**

```bash
cd apps/user-web
npm run dev
```

验证：
- 访问 `http://localhost:5174/delivery-mall` 显示合并商品列表
- 列表同时包含平台自营商品和供货商品（如果有的话）
- 卡片样式统一，不区分来源
- 点击商品可查看详情

- [ ] **Step 4: 提交**

```bash
git add apps/user-web/src/pages/DeliveryMallPage.vue apps/user-web/src/api/mall.js
git commit -m "feat(supply): merge supply products into delivery mall with UNION query"
```

---

### Task 15: Python 通知事件扩展

**Files:**
- Modify: `apps/automation-service/app/services/notify_dispatcher.py`
- Reference: 现有事件常量定义（第 68-76 行）

- [ ] **Step 1: 阅读现有事件定义**

阅读 `apps/automation-service/app/services/notify_dispatcher.py` 第 68-76 行，了解事件常量命名规范（`EVENT_XXX`）和事件列表结构。

- [ ] **Step 2: 新增 2 个事件常量**

在事件常量定义区域新增：

```python
EVENT_SUPPLY_AUDIT_APPROVED = "supply_audit_approved"      # 货源审核通过
EVENT_SUPPLY_AUDIT_REJECTED = "supply_audit_rejected"      # 货源审核驳回
```

在事件列表中追加：

```python
EVENT_DISPLAY_NAMES = {
    # ... 现有事件 ...
    EVENT_SUPPLY_AUDIT_APPROVED: "货源审核通过",
    EVENT_SUPPLY_AUDIT_REJECTED: "货源审核驳回",
}
```

注意：Phase 1 仅新增这 2 个事件，其余 7 个交易相关事件在 Phase 2/3 新增。

- [ ] **Step 3: 提交**

```bash
git add apps/automation-service/app/services/notify_dispatcher.py
git commit -m "feat(notify): add supply audit approved/rejected event types"
```

---

### Task 16: Phase 1 端到端验证

**Files:**
- 无新建，全流程验证

- [ ] **Step 1: 启动全部服务**

```bash
# 在项目根目录
powershell -ExecutionPolicy Bypass -File .\dev-start.ps1 -NoPause
```

确认 5 个服务全部启动：core-api (18080)、automation-service (12401)、crawler-service (3001)、admin-web (3006)、user-web (5174)。

- [ ] **Step 2: 完整流程验证**

在 user-web 中：
1. 登录前台（admin/123456）
2. 访问"供货中心"页面，确认统计卡片显示
3. 点击"上传货源"，填写信息并提交
4. 确认列表显示"待审核"状态
5. 确认收到站内信通知（如有）

在 admin-web 中：
6. 登录后台（admin/123456）
7. 访问"审核工作台"
8. 确认看到待审核的供货商品
9. 点击"通过"，确认审核成功

回到 user-web：
10. 刷新货源列表，确认状态变为"已通过"
11. 点击"上架"
12. 访问"货源商城"，确认上架的商品出现在列表中（与平台自营合并展示）

- [ ] **Step 3: 验证驳回流程**

重复上传一个货源，在后台点击"驳回"并填写原因，验证：
- 前台货源状态变为"已驳回"
- 显示驳回原因
- 站内信收到驳回通知

- [ ] **Step 4: 验证权重调整**

在后台"权重调整"页面：
- 调整某个商品的 weight 值
- 回到前台货源商城，确认商品顺序按 weight 排序

- [ ] **Step 5: 提交最终修复（如有）**

```bash
git add -A
git commit -m "test(supply): Phase 1 end-to-end verification passed"
```

---

## Phase 2 & 3 概要（待 Phase 1 完成后细化）

### Phase 2：交易订单与资金托管

**新建表**：trade_order + trade_balance + cash_balance_ledger + trade_frozen_record
**新建服务**：TradeOrderService + TradeEscrowService + TradeBalanceService + TradeScheduleService
**关键任务**：
1. 建表迁移脚本 V1.68
2. TradeBalanceService 余额操作（行锁 + 流水）
3. TradeOrderService 下单 + 支付回调 + 发货（复用 CardService.claimUnusedCard）
4. TradeEscrowService 资金冻结 + 解冻 + 抽佣分账
5. TradeScheduleService 定时任务（冷冻期解冻 + 自动确认收货）
6. PaymentService 扩展支持 order_type='supply_product'
7. 前端：货源商城购买流程 + 订单管理 + 供货中心主页收入/余额展示
8. 通知：新增 4 个事件（新订单/发货/确认收货/资金解冻）

### Phase 3：提现与退款

**新建表**：trade_withdrawal_request + trade_refund
**新建服务**：TradeWithdrawalService + TradeRefundService
**关键任务**：
1. 建表迁移脚本 V1.69
2. TradeWithdrawalService 提现申请 + 审核 + 打款标记
3. TradeRefundService 退款申请 + 卖家响应 + 退款延长冷冻期逻辑
4. TradeScheduleService 扩展：退款结束后恢复解冻
5. 前端：提现申请页 + 退款申请/详情页（含客服微信提示）
6. 后台：提现审核页 + 退款记录查看页
7. 通知：新增 3 个事件（退款申请/退款处理结果/提现结果）

---

## Self-Review

**1. Spec coverage（设计文档覆盖检查）**：
- ✅ 货源上传与管理（Task 3, 7, 12）
- ✅ 审核机制（Task 4, 9, 13）
- ✅ 货源商城合并展示 + 权重（Task 6, 8, 14, 9）
- ✅ 供货中心首页（Task 3.dashboard, 12）
- ✅ 通知场景（Task 5, 15）
- ⏳ 交易结算（Phase 2）
- ⏳ 7 天冷冻期（Phase 2）
- ⏳ 提现（Phase 3）
- ⏳ 退款（Phase 3）

**2. Placeholder scan**：
- 无 TBD/TODO
- 前端 Vue 组件因篇幅原因给出实现要点而非完整代码（遵循现有页面模式编写，有明确参考文件）

**3. Type consistency**：
- `SupplyProductService` 方法名与 `SupplyProductController` 路由一致
- `SupplyAuditService.approve/reject` 与 `AdminSupplyAuditController` 调用一致
- 前端 API 函数名与后端路由对应

---

## Execution Handoff

Plan complete and saved to `docs/2026-07-31-供货中心-Phase1-实施计划.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
