# 上传前验证快照

> 生成时间：2026-08-13
> 目的：固化统一上传前的最终基线，供发布前快速核对。

## 一、总体结论

- 交叉功能强化：13 项，全部只修改我方项目（`G:\源码\xianyu-assistant-package-temp`）
- 同类项目（`G:\源码\垃圾箱\xianyu-auto-reply`）：仅读取，未做任何修改
- 待办：等待用户授权统一上传，然后按发布清单执行线上部署与巡检

## 二、工作区文件状态

- 变更/新增文件总数：66
- 其中与本任务无关的既有改动（发布前需用户确认）：
  - `scripts/deploy-crawler-service.py`
  - `scripts/quick-deploy-automation.py`
- 与任务无关的既有未跟踪临时文件（建议忽略）：
  - `_tmp_domdump.py`
  - `_tmp_dragtest.py`

## 三、验证命令与结果

| 项目 | 命令 | 结果 |
|------|------|------|
| 数据库迁移 | `python scripts/validate_migrations.py` | 通过：110 项，sha256=`6db0c519db0d285447219e72729410730741a241036717cbe1304a431426d0cc` |
| Python 测试 | `pytest`（automation-service 相关套件） | 183 通过 |
| Java 编译 | `mvnw -q compile -DskipTests`（core-api） | 通过 |
| 前端构建 | `npm run build`（user-web） | 通过（含 dist-safety-check） |
| 接口冒烟 | automation-service / core-api 本地启动 | 消息过滤、默认回复、黑名单、拦截规则、退款关单、流量分布、素材移除后无残留等均通过 |

## 四、数据库迁移清单

- automation_mysql：V1.29 / V1.30 / V1.31 / V1.32 / V1.33 / V1.34
- core_mysql：本轮无新增（V1.76 发布素材库已按用户要求移除）

## 五、发布命令（待用户确认）

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy-prod.ps1 `
  -Target all `
  -ReleaseId 20260813-all `
  -MigrationEvidence 6db0c519db0d285447219e72729410730741a241036717cbe1304a431426d0cc
```

> 若上传前再次修改迁移/清单，请重新执行 `python scripts/validate_migrations.py` 获取最新 sha256。
