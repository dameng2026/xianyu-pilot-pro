# 统一上传发布清单

> 生成日期：2026-08-13
> 状态：功能强化已全部完成，等待用户确认后统一上传

## 一、本次强化清单

| # | 功能 | 涉及端 | 迁移 |
|---|------|--------|------|
| 1 | 消息过滤规则（跳过自动回复/通知） | automation-service + core-api 代理 + user-web | V1.29（automation） |
| 2 | 默认回复（文本/图片/API/仅回复一次） | automation-service + core-api 代理 + user-web | V1.30（automation） |
| 3 | 自动回复规则管理页 + 命中日志 | user-web（复用既有 Java CRUD） | - |
| 4 | 自动回复多消息拆分 + 变量 | automation-service | - |
| 5 | 自动回复图片关键词 + 商品绑定 | automation-service + core-api + user-web | V1.31（automation） |
| 6 | 自动回复规则 CSV 导入/导出 | user-web | - |
| 7 | 发货“先确认再发内容”开关 | automation-service + user-web | - |
| 8 | 发货买家黑名单 | automation-service + core-api 代理 + user-web | V1.32（automation） |
| 9 | 发货拦截规则引擎（已有订单/未确认收货） | automation-service + core-api 代理 + user-web | V1.33（automation） |
| 10 | 流量分布（来源/商品/时间/地域） | automation-service + core-api 代理 + user-web | - |
| 11 | 求小红花（订单详情一键发送并标记） | core-api + automation-service（复用 sendMessage）+ user-web | - |
| 12 | 退款关单（退款订单注销回调） | automation-service + core-api 代理 + user-web | V1.34（automation） |
| 13 | 订单已关闭仍补发（card_only） | automation-service + user-web | - |

## 二、需要执行的数据库迁移

- automation_mysql：V1.29 / V1.30 / V1.31 / V1.32 / V1.33 / V1.34

发布工具 `scripts/deploy-prod.ps1 -Target all` 会按 `db/migrations-manifest.json` 校验并应用（当前 110 项迁移，已通过本地校验）。

## 三、发布命令

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy-prod.ps1 `
  -Target all `
  -ReleaseId 20260813-all `
  -MigrationEvidence <db/migrations-manifest.json 的 sha256>
```

说明：
- `-ReleaseId` 格式：字母/数字开头，2–127 位，可含 `._-`（示例 `20260813-all`）
- `-MigrationEvidence` 必须是 `db/migrations-manifest.json` 当前文件的 64 位小写 sha256；
  上传前重新执行 `python scripts/validate_migrations.py` 获取最新值
- 当前基线 sha256：`6db0c519db0d285447219e72729410730741a241036717cbe1304a431426d0cc`
- 如后续再改迁移/清单，请以最新校验输出为准

## 四、发布后巡检

- 后端健康：`http://211.161.232.54:18080/api/health`
- 管理端健康：`http://211.161.232.54:18080/admin-api/health`
- 用户端：`http://64.90.31.68:81/#/login`
- 管理端：`http://64.90.31.68:82/#/auth/login`
- 功能巡检：
  - 消息过滤：新增关键词规则 → 命中消息不自动回复/不弹新消息
  - 默认回复：关闭 AI 客服后，未命中规则的消息收到兜底回复
  - 回复规则：新增/编辑/启停/预览/导入导出均可用
  - 图片关键词：上传图片并绑定商品，命中关键词后收到图片回复
  - 发货顺序：开启“先确认再发内容”后，确认失败不发送
  - 黑名单：拉黑买家（单账号或全部账号）后下单不自动发货
  - 拦截规则：开启“已有订单”后，多单买家被拦截
  - 流量分布：选择鱼小铺账号查询近1/7/30天与自定义范围，四类分布正常展示
  - 求小红花：已完成且未标记的订单点击「求小红花」发送成功并标记；已标记订单按钮不可用
  - 退款关单：配置账号开关与 https URL，触发退款订单同步后外部接口收到回调并标记注销
  - 关闭补发：开启“订单已关闭仍补发”后，先确认发货遇到订单关闭时不中止，继续补发内容
- 日志：观察 core-api / automation-service 日志，确认无 `bad SQL grammar`、`Unhandled exception`、重复 4xx/5xx

## 五、本地终检结果（发布前基线）

- Python 测试：183 通过（含消息过滤、默认回复、多消息拆分、黑名单、拦截规则、先确认后发卡、流量分布、退款关单、关闭补发等）
- core-api Maven 编译：通过
- user-web 构建：通过
- 迁移清单：110 项校验通过
- 本地接口冒烟：消息过滤 CRUD、默认回复 CRUD、黑名单 CRUD/命中、拦截规则 CRUD 均通过

## 六、工作区注意事项

- 同类项目 `G:\源码\垃圾箱\xianyu-auto-reply` 仅被读取，未做任何修改。
- 工作区存在 2 个与本任务无关的既有改动：
  - `scripts/deploy-crawler-service.py`（CHANGED_FILES 列表改动）
  - `scripts/quick-deploy-automation.py`（CHANGED_FILES 列表改动）
  - 它们不是本次强化内容，但若上传工具整仓同步会一并携带；建议发布前由用户确认是否保留。
