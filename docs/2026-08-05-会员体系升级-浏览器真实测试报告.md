# 会员体系升级 - 浏览器真实测试报告

日期：2026-08-05
测试方式：Playwright CLI 驱动真实 Chromium 浏览器，访问本地真实服务（core-api 18080 / user-web 5174 / admin-web 3006），覆盖前台会员中心、个人中心、账号管理、后台功能管理与用户管理，并对后端接口、Python 自动化侧逻辑做真实调用验证。

---

## 一、测试环境

| 服务 | 地址 | 说明 |
|---|---|---|
| core-api | http://localhost:18080 | 最新源码启动（含本次会员改造与测试中修复） |
| user-web（开发态） | http://localhost:5174 | Vite dev，代理 /api 到 18080 |
| admin-web（开发态） | http://localhost:3006 | Vite dev，代理 /admin-api 到 18080 |
| MySQL | localhost:3306 | 本地开发库，已执行 V1.69 迁移 |

测试账号（测试后已清理）：
- `demo`（普通用户，1 个店铺，达到 1 店上限）
- `e2e_normal`（普通用户，2 个店铺，超限）
- `e2e_vipsingle`（VIP 单店版，1 个店铺，达到上限）
- `e2e_vip`（VIP，2 个店铺，不限）
- `e2e_svip`（SVIP，2 个店铺，不限）

---

## 二、测试结果总览

| 模块 | 用例 | 结果 |
|---|---|---|
| 前台会员中心 | 只展示 3 个可购套餐（单店版 9.99 / VIP 19.99 / SVIP 39.99），普通用户套餐不再出现 | ✅ |
| 前台会员中心 | 单店版介绍含“闲鱼店铺数量：1 个”；VIP/SVIP 含“不限制” | ✅ |
| 前台会员中心 | 功能对比表四列：普通用户 / VIP（单店版）/ VIP会员 / SVIP会员，无“限制模式”列 | ✅ |
| 前台会员中心 | 首行“店铺数量”：1 / 1 / 不限 / 不限 | ✅ |
| 前台会员中心 | 排序：可用等级少的排后（商品草稿箱、图片生成记录“仅 SVIP 可用”排自动化组最后） | ✅ |
| 前台个人中心 | 对比表四列 + 店铺数量首行 + 排序 | ✅ |
| 前台个人中心 | “你的等级”标识显示在当前用户等级列（普通用户 → 普通用户列） | ✅（本次修复） |
| 前台账号管理 | 普通用户点“扫码加账号” → 弹窗“店铺数量已达上限”，不打开扫码弹窗 | ✅ |
| 前台账号管理 | 普通用户点“手动添加” → 同上弹窗 | ✅ |
| 前台账号管理 | 弹窗点“去升级会员” → 跳转会员中心 #/vip | ✅ |
| 前台账号管理 | VIP 用户点“手动添加” → 直接打开手动添加弹窗（不被拦截） | ✅ |
| 移动端账号管理 | 普通用户点“添加账号” → 立即弹出升级引导确认框（修复前需第二步才拦截） | ✅（本次修复） |
| 移动端账号管理 | VIP 用户点“添加账号” → 打开添加方式选择（不被拦截） | ✅ |
| 后端接口 | `/api/feature-switches/store-limit`：5 个等级返回正确 limit/count | ✅ |
| 后端接口 | 普通/单店版超限添加账号 → 400 `STORE_LIMIT_REACHED` | ✅ |
| 后端接口 | VIP/SVIP 添加账号 → 成功创建并可删除 | ✅ |
| Python 自动化 | `store_limit` 模块 5 个等级限制计算正确（含 vip_level=3 映射） | ✅ |
| 后台功能管理 | 表头 9 列，含“VIP（单店版）”列；功能行单店版为“同VIP”联动只读 | ✅ |
| 后台功能管理 | 首行“店铺数量”四个数字输入框 1/1/0/0 | ✅ |
| 后台功能管理 | 修改店铺数 → 保存成功 → 接口确认持久化 → 恢复原值 | ✅ |
| 后台用户管理 | 用户列表等级：普通用户 / VIP（单店版）/ VIP / SVP 显示正确 | ✅ |
| 后台用户管理 | 编辑弹窗 VIP 级别下拉包含：普通用户 / VIP（单店版）/ VIP / SVP | ✅ |

---

## 三、本次测试发现并修复的问题

### 1. 添加账号 500：`snapshot_time` 无默认值（存量 bug，阻断所有账号新增）

现象：通过 API 手动添加账号时，Java 后端返回 500“账号保存失败，数据库异常”。

根因：`xianyu_account_health_snapshot.snapshot_time` 为 `NOT NULL` 且无默认值，`XianyuAccountHealthSnapshotMapper.insert` 未写入该字段，导致新增账号（含扫码/手动）整体失败。

修复：
- `XianyuAccountHealthSnapshotMapper`：INSERT 补充 `snapshot_time`（与 `collected_time` 同值）。
- `SchemaCompatibilityRunner`：建表语句补充 `snapshot_time DATETIME DEFAULT CURRENT_TIMESTAMP`，并对存量表执行幂等 `ALTER ... SET DEFAULT (CURRENT_TIMESTAMP)`。

复测：普通/VIP/SVIP 手动添加均正常；超限拦截与新增成功均符合预期。

### 2. 移动端“添加账号”拦截时机滞后（本次改造引入的体验缺口）

现象：移动端普通用户点“添加账号”先打开“扫码登录 / 手动 Cookie”选择面板，选择具体方式后才弹升级引导。

修复：主“添加账号”按钮改为 `onAddAccountClick()`，先调用 `ensureCanAddStore()`，未超限才打开选择面板。

复测：普通用户点击“添加账号”立即弹出升级引导确认框；VIP 用户正常打开选择面板。

### 3. 个人中心“你的等级”标识固定显示在 SVIP 列（存量 UX 问题）

现象：无论当前用户等级，功能对比表 SVIP 列都显示“你的等级”，信息不准确。

修复：按 `overview.activePlan.planCode` 计算当前等级列，标识显示在对应列（普通用户 / VIP 单店版 / VIP / SVIP）。

复测：普通用户登录后，“你的等级”显示在“普通用户”列。

---

## 四、环境性/历史问题（非本次改造引入，已记录）

1. admin-web 首次启动会话出现 Vite “Outdated Optimize Dep” 504，导致用户管理模块首屏加载失败；重启 dev server 并新开浏览器会话后正常。属本地开发环境依赖缓存问题，与业务代码无关。
2. 前台 SSE 在 Vite dev 代理下返回 `text/plain`，浏览器控制台报 EventSource MIME 错误；生产环境由 Nginx 正确返回 `text/event-stream`，不影响业务。
3. 首页轮播图未配置时请求图片 404（后台未配置内容）。
4. 后台“数据管理”路由缺少组件（ComponentLoader 报错），为既有未实现模块。
5. 本地仓库 HEAD 上 `FeatureSwitchServiceTest` 2 个用例与 `XianyuAccountServiceTest` 1 个用例本就失败（已用临时 worktree 对照确认），与本次改动无关。

---

## 五、结论

本次会员体系升级（普通用户 / VIP（单店版）/ VIP / SVIP）已通过真实浏览器与真实接口回归：
- 前台展示、排序、店铺数量配置、升级引导弹窗全部符合预期；
- 后台功能管理四档配置与用户等级管理可用；
- 后端与 Python 侧店铺数量强制校验一致；
- 测试中发现并修复 1 个存量账号新增 500 问题、1 个移动端拦截时机问题、1 个个人中心等级标识问题。

建议上线前在生产环境重复执行 V1.69 迁移、部署四个服务，并按本报告用例做一次冒烟回归。

---

## 六、生产上线记录（2026-08-05）

### 发布信息

| 项 | 值 |
|---|---|
| Git 提交 | `fdd726b0bda73c26fa68ab81af1492d43d470a61`（master） |
| 发布 ID | `release-20260805-membership-01` |
| 迁移 | `V1.69__membership_vip_single_store_limit.sql`（已在生产 MySQL 执行并校验） |
| 部署方式 | `scripts/deploy-prod.ps1 -Target all`（官方部署脚本，含远端预检、回滚保护、自动冒烟） |
| 部署结果 | 成功，官方冒烟全部通过（用户/管理端健康、首页、登录、鉴权会话） |

### 生产验证

- 中国后端健康：`/api/health`、`/admin-api/health` 均 `UP`。
- 用户/管理端登录 API 均 `code=200`，token 可访问受保护接口。
- `GET /api/feature-switches/store-limit`（SVIP 账号）：`limit=0, unlimited=true`。
- `GET /api/feature-switches/comparison`：46 项，首行 `store-limit` = 1/1/0/0，功能行 `vipSingle` 与 `vip` 一致。
- `GET /api/billing/plans`：normal（免费/1 店）、vip-single（¥9.99/1 店）、vip（¥19.99/不限）、svp（¥39.99/不限）。
- 后台 `GET /admin-api/system/feature-switches`：store-limit 行与 VIP（单店版）联动字段正确。
- 真实浏览器：用户前台登录成功，会员中心展示 3 个套餐、四列对比表、首行店铺数量 1/1/不限/不限；后台登录页正常加载。
- 后端日志无新增 `bad SQL grammar` / 未处理异常；自动化服务仅有 3 条瞬时 `PublicRuntimeError`（20:15 探测期，未再复现）与 1 条既有账号 WS Token 失败，均与本次会员改造无关。

### 上线操作留痕

- 生产库已备份：`/home/ubuntu/db-backups-pre-release/backup_mysql_20260804T200650Z.sql`、`backup_crawler_20260804T200650Z.dump`。
- 迁移证据：本地 `C:\secure\xianyupilot\migration-evidence.json`，远端 `/home/ubuntu/release-evidence/migration-evidence-20260805-001.json`。
- 服务器 `.env.production` 的 `RELEASE_ID/RELEASE_REVISION/DATABASE_MIGRATION_EVIDENCE_FILE` 已绑定本次发布。
- 8 月 6 日 24:00 店铺超限清理定时任务已随新后端上线，默认在 `2026-08-07 00:00`（Asia/Shanghai）后首次调度执行。

---

## 七、第二轮修复与上线（2026-08-05 二次发布）

### 1. 线上中文乱码修复

现象：后台套餐管理、前台会员中心/个人中心出现 `åº—é“ºæ•°é‡` 乱码。

根因：V1.69 迁移 SQL 在生产库执行时，mysql 客户端未指定 `--default-character-set=utf8mb4`，UTF-8 中文被按 Latin-1 双重编码后写入（`billing_plan.plan_name/features_text`、`store-limit.title`）。

修复：使用 `--default-character-set=utf8mb4` 客户端以 UTF-8 文件方式重写生产数据，并核验 HEX 为正确 UTF-8（`店铺数量` = E5BA97E993BAE695B0E9878F 等）。

### 2. 会员开通/到期时间展示

- 数据：`billing_subscription.start_time/end_time` 已由支付流程写入，无需新增字段。
- 前台会员中心：会员信息卡新增「开通时间」「到期时间」。
- 前台个人中心：用户信息新增「会员开通时间」「会员到期时间」；到期提醒卡显示开通时间。
- 后台用户管理：用户列表新增「会员开通」「会员到期」两列。
- 手动等级覆盖（如后台直接设置 VIP/SVP）时，等级仍以手动为准，但开通/到期时间展示真实订阅时间。

### 3. 功能对比表优化

- 移除「概览 N 项 / 账号与商品 N 项」等分组汇总行，只保留功能行。
- 所有功能按可用等级数全局自动排序：4 个等级可用排最前，0 个可用排最后；店铺数量行固定在首行。

### 4. 二次上线记录

| 发布 | 提交 | 内容 |
|---|---|---|
| release-20260805-membership-02 | `5c4bfec` | 乱码数据修复 + 开通/到期时间展示 + 对比表去分组行/全局排序（前后端全量） |
| release-20260805-membership-03 | `f2ef6be` | 手动等级覆盖时仍展示真实订阅开通/到期时间（后端） |

两次发布均通过官方部署脚本远端预检、容器健康门禁与公共冒烟（健康、首页、用户/管理端登录、鉴权会话）。

### 5. 线上复验结果

- 会员中心：VIP（单店版）¥9.99 / VIP ¥19.99 / SVIP ¥39.99 正常中文展示；对比表 46 行、分组行 0；首行店铺数量 1/1/不限/不限；会员信息含开通/到期时间。
- 个人中心：对比表同结构；用户信息含「会员开通时间」「会员到期时间」。
- 后台用户列表 API：订阅用户返回 `planStartTime/planEndTime`（示例：2026-07-31 ~ 2026-08-30）。
- `/api/billing/plans`、`/api/feature-switches/comparison` 返回正确 UTF-8 中文与 store-limit 配置。
